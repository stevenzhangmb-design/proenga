# 测试用例模版（参考 IEEE 829 + ISTQB 测试设计标准）

> 用途：基于 prd-data.json + 原型 HTML 生成的测试用例文档。
>
> 适用范围：B 端 / C 端 / 移动 / 小程序 / SaaS / IoT 等任意产品形态。
>
> 占位符约定：`<尖括号>` = 待填充。

---

# <模块/系统名> 测试用例

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 文档名称 | <模块名> 测试用例 |
| 版本 | v1.0 |
| 日期 | YYYY-MM-DD |
| 作者 | <作者> |
| 关联 PRD | ./PRD-XXX-<日期>.md |
| 关联原型 | ./原型-XXX-<日期>-offline.html |
| 关联数据源 | ./prd-data-XXX-<日期>.json |
| 关联技术设计 | ./技术概要设计-XXX-<日期>.md |
| 变更日志 | <与上一版变更点> |

## 2. 测试范围

### 2.1 功能范围
按 menus 结构列出本次测试覆盖的功能点：

| 模块 | 功能点 | 是否覆盖 |
|---|---|---|
| <子模块 1> | 查询 / 新增 / 编辑 / 删除 / 启用 / 停用 | ✅ |
| <子模块 2> | <fp 列表> | ✅ |

### 2.2 不在范围
- <列出不测的内容，如：① 第三方依赖系统 ② 已通过测试的存量功能>

### 2.3 测试环境
- 测试环境：<URL / IP / 账号>
- 测试数据：<数据集说明>
- 浏览器：Chrome 最新版 + Edge 最新版 + Safari 最新版
- 屏幕：1920×1080 / 1440×900 / 1366×768
- 移动端（如适用）：iOS 16+ / Android 13+

## 3. 测试用例（按功能点组织）

> **用例字段说明**：
> - **优先级**：P0（核心，必过）/ P1（重要）/ P2（次要）/ P3（边缘）
> - **类型**：功能 / 性能 / 兼容 / 安全 / 易用
> - **设计方法**：等价类 / 边界值 / 错误推测 / 因果图 / 决策表 / 状态转换

---

### 3.1 <功能点 1：查询>

#### 3.1.1 正向用例

| 用例编号 | 用例名称 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 |
|---|---|---|---|---|---|---|
| TC-<模块编号>-001 | 全条件查询 | P0 | 1.用户已登录<br>2.具备「<菜单路径>-查询」权限 | 1.进入列表页<br>2.填写所有筛选条件<br>3.点查询按钮 | 名称="<示例>"<br>状态="已启用" | 列表只显示符合条件的数据，分页保持 |
| TC-<模块编号>-002 | 单条件查询 | P1 | 同上 | 1.填一个筛选条件<br>2.点查询 | <仅填名称> | 列表按该条件筛选 |
| TC-<模块编号>-003 | 无条件查询 | P1 | 同上 | 1.所有筛选留空<br>2.点查询 | 全部留空 | 列表显示全部数据 |
| TC-<模块编号>-004 | 重置功能 | P1 | 已有筛选结果 | 点重置按钮 | — | 筛选条件清空 + 列表恢复全部 |
| TC-<模块编号>-005 | 分页翻页 | P1 | 数据总数 > pageSize | 1.点下一页<br>2.点指定页码<br>3.跳页输入 | 数据 ≥ 11 条 | 列表按页切换正确 |
| TC-<模块编号>-006 | 切换每页条数 | P2 | 同上 | 切换 10/25/50/100 | — | 列表按新条数显示 + 自动回第 1 页 |
| TC-<模块编号>-007 | 列表排序 | P2 | 同上 | 点击表头排序 | — | 按字段升降序排列 |

#### 3.1.2 边界值用例（按 field_specs[].constraint）

| 用例编号 | 字段 | 边界类型 | 输入值 | 预期 |
|---|---|---|---|---|
| TC-BV-001 | <文本字段> | 最小长度 | "<1 字符>" | 通过 |
| TC-BV-002 | <文本字段> | 最大长度 | "<N 字符>" | 通过 |
| TC-BV-003 | <文本字段> | 超长 | "<N+1 字符>" | 失焦时字段下方红字「长度 X~N 字符」|
| TC-BV-004 | <文本字段> | 空 | "" | 必填字段：失焦红字「请输入<字段名>」；非必填：通过 |
| TC-BV-005 | <文本字段> | 特殊字符 | "<XSS / SQL注入>" | 不报错 + XSS / SQL 防护 |
| TC-BV-006 | <数值字段> | 最小值 | <min> | 通过 |
| TC-BV-007 | <数值字段> | 最大值 | <max> | 通过 |
| TC-BV-008 | <数值字段> | 超下限 | <min-0.01> | 红字「范围 X~Y」|
| TC-BV-009 | <数值字段> | 超上限 | <max+0.01> | 红字「范围 X~Y」|
| TC-BV-010 | <数值字段> | 小数位 | <超过 precision> | 自动截断 / 红字 |
| TC-BV-011 | <日期字段> | 边界日期 | 起始日期 / 结束日期 | 通过 |

#### 3.1.3 异常 / 反向用例

| 用例编号 | 场景 | 测试步骤 | 预期 |
|---|---|---|---|
| TC-EX-001 | 未登录 | 直接访问列表 URL | 跳转登录页 |
| TC-EX-002 | 无权限 | 普通用户访问 | 按钮置灰 / 403 错误 |
| TC-EX-003 | 网络中断 | 查询时断网 | 提示「网络异常，请重试」|
| TC-EX-004 | 服务端 500 | 模拟后端错误 | 友好错误提示 + 不白屏 |
| TC-EX-005 | 数据为空 | 数据库无符合条件 | 空状态展示「暂无数据」|

---

### 3.2 <功能点 2：新增>

#### 3.2.1 正向用例（按 use_cases.operation_flow 分步）

| 用例编号 | 步骤 | 操作 | 预期 |
|---|---|---|---|
| TC-<编号>-101 | Step 1 | 点新增按钮 | 弹窗打开，编号自动生成（如 SPU000001）|
| TC-<编号>-102 | Step 2 | 填表 + 点保存 | 弹二次确认 |
| TC-<编号>-103 | Step 3 | 点确认 | 弹窗关闭 + 列表多 1 行 + 成功 Toast |
| TC-<编号>-104 | Step 4 | 点取消 | 弹窗关闭 + 列表不变 |
| TC-<编号>-105 | Step 4 | 点 × 关闭 | 弹窗关闭 + 列表不变 + 校验状态清空 |
| TC-<编号>-106 | Step 4 | 按 ESC | 弹窗关闭 + 列表不变 |

#### 3.2.2 字段级校验用例（按 field_specs.constraint）

| 用例编号 | 字段 | 校验项 | 触发时机 | 输入 | 预期 |
|---|---|---|---|---|---|
| TC-FV-001 | <必填字段> | required | blur 失焦 | 留空 | 字段下方红字「请输入<字段名>」|
| TC-FV-002 | <必填字段> | required | submit 提交 | 留空 | 字段下方红字 + 阻止提交 |
| TC-FV-003 | <唯一字段> | unique | blur 失焦 | 输入已存在值 | 字段下方红字「<字段名>已存在」|
| TC-FV-004 | <格式字段> | pattern | blur 失焦 | 不合法格式 | 字段下方红字「<格式说明>」|
| TC-FV-005 | <跨字段比较> | cross-field | blur 失焦 | <比较失败> | 字段下方红字「<比较规则>」|

#### 3.2.3 业务前置校验用例（按 use_cases.validations）

| 用例编号 | 校验类型 | 测试场景 | 预期 |
|---|---|---|---|
| TC-BV-001 | 关联依赖 | <例：删除有 N 个子数据的对象> | 拦截 + Toast「有 N 个关联，不可删除」|
| TC-BV-002 | 状态机 | <例：启用已启用对象> | 拦截 + Toast「已启用，无需重复」|
| TC-BV-003 | 唯一性 | <例：新增重名> | 拦截 + 红字「已存在」|
| TC-BV-004 | 时效 | <例：超 90 天调拨单撤销> | 拦截 + Toast「超期不可撤销」|

#### 3.2.4 并发用例

| 用例编号 | 场景 | 预期 |
|---|---|---|
| TC-CC-001 | 同名并发新增 | 第二个失败 + 唯一性约束拦截 |
| TC-CC-002 | 同对象并发编辑 | 后者乐观锁失败 + 提示「数据已被他人修改」|
| TC-CC-003 | 删除时并发引用 | 删除失败 + 提示关联依赖 |

---

### 3.3 <功能点 3：编辑>

（同 3.2 结构，增加"回显数据"验证）

| 用例编号 | 场景 | 预期 |
|---|---|---|
| TC-<编号>-201 | 数据回显 | 弹窗打开后所有字段按当前行数据回显 |
| TC-<编号>-202 | 部分修改 | 只改一个字段 + 保存 | 仅该字段更新，其它字段不变 |
| TC-<编号>-203 | 不可改字段 | 编号字段置灰，无法编辑 |

---

### 3.4 <功能点 4：删除>

（含业务前置校验、二次确认、软删除验证）

---

### 3.5 <功能点 5：启用 / 停用>

（含状态机校验、Tag 切换验证、状态对其他业务的影响）

---

### 3.6 <其它自定义功能点>

按 prd-data.json 中 fp_type 自动生成对应用例集（如：上架/下架/审核/驳回/打印/复制/导入/导出/调拨 等）。

## 4. 端到端业务场景用例

> 模拟真实用户的完整业务流程，验证多功能点协同。

| 场景编号 | 场景名称 | 涉及功能点 | 操作步骤 | 预期 |
|---|---|---|---|---|
| E2E-001 | <对象>全生命周期 | 新增 → 编辑 → 启用 → 停用 → 删除 | <逐步描述> | 每步状态正确 + 操作日志完整记录 |
| E2E-002 | 批量导入 → 批量编辑 → 导出 | 导入 / 编辑 / 导出 | <逐步> | 数据一致 |
| E2E-003 | 跨模块业务联动 | <如：商品上架 → 库存初始化 → 价格策略生效> | <逐步> | 多模块数据同步 |

## 5. 非功能测试

### 5.1 兼容性

| 维度 | 覆盖 | 用例数 |
|---|---|---|
| 浏览器 | Chrome / Edge / Firefox / Safari 最新 2 个版本 | <X> |
| 屏幕分辨率 | 1920×1080 / 1440×900 / 1366×768 | <X> |
| 移动端 | iOS 16+ / Android 13+ | <X> |
| 网络环境 | 4G / 3G / 弱网 | <X> |

### 5.2 性能

| 用例编号 | 场景 | 指标 | 预期 |
|---|---|---|---|
| PERF-001 | 列表加载 10 条 | 响应时间 | < 200ms |
| PERF-002 | 列表加载 1000 条 | 响应时间 | < 2s |
| PERF-003 | 并发查询 100 用户 | TPS | > 500 |
| PERF-004 | 批量导入 10k 条 | 完成时间 | < 60s |
| PERF-005 | 并发新增 50 个对象 | 成功率 | 100% |

### 5.3 安全

| 用例编号 | 场景 | 预期 |
|---|---|---|
| SEC-001 | SQL 注入 | 所有输入做参数化 / 过滤，不被注入 |
| SEC-002 | XSS | HTML 转义，不执行恶意脚本 |
| SEC-003 | CSRF | Token 校验，跨站请求被拦截 |
| SEC-004 | 越权（水平 / 垂直）| 无权限用户访问被拒绝 |
| SEC-005 | 敏感数据脱敏 | 手机号 / 身份证 / 银行卡 显示脱敏 |
| SEC-006 | 暴力破解 | 登录失败 N 次锁定 |

### 5.4 易用性

| 用例编号 | 场景 | 预期 |
|---|---|---|
| UX-001 | 错误提示清晰 | 字段下方红字直接指出问题 |
| UX-002 | 操作反馈及时 | 点击按钮 200ms 内有视觉反馈 |
| UX-003 | 必填标识清晰 | * 红色标识 |
| UX-004 | 危险操作二次确认 | 删除 / 状态改变弹确认 |
| UX-005 | 撤销 / 取消可逆 | 取消按钮恢复初始状态 |

## 6. 用例统计

| 优先级 | 数量 | 说明 |
|---|---|---|
| P0 | <X> | 核心，必须 100% 通过 |
| P1 | <X> | 重要，> 95% 通过 |
| P2 | <X> | 次要，> 85% 通过 |
| P3 | <X> | 边缘 |
| **合计** | <X> | — |

| 类型 | 数量 |
|---|---|
| 功能 | <X> |
| 性能 | <X> |
| 安全 | <X> |
| 兼容 | <X> |
| 易用 | <X> |

## 7. 用例执行追踪

| 用例编号 | 执行人 | 执行日期 | 状态 | Bug 编号 | 备注 |
|---|---|---|---|---|---|
| TC-XXX-001 | <执行人> | YYYY-MM-DD | 通过 / 失败 / 阻塞 | <Bug 编号> | <备注> |

## 8. 附录

- 测试数据集：`./test-data/<模块>.json`
- 测试脚本：`./tests/<模块>/`（自动化测试代码）
- 缺陷清单：`./bugs/<模块>-bugs.md`
