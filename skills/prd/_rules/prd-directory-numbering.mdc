---
description: PRD 第 4 章目录与 4.4 多级编号（系统→菜单→功能点→位置/原型/字段/用例）约定，适用于所有产品形态（ERP/WMS/零售/APP/小程序/H5/PC网站）
globs: "**/PRD*.md"
alwaysApply: false
---

# PRD 目录与 `4.4.*` 编号约定

> 🏛 **本文件为 PRD 第 4 章编号约定的单一真理源**（与同目录 `prd-template-structure.mdc` 并列宪法层）。
> SKILL.md / 提示词模板 / 示例如有不一致，以本文件为准。
>
> **适用范围**：所有产品形态，包括但不限于 ERP / WMS / 零售门店 / SaaS / APP / 小程序 / H5 / PC 网站。层级深度与术语叫法以**实际原型结构**为准，不强行套固定层数（详见下方§产品形态自适应规则）。

编写或调整需求文档第 4 章时遵守以下结构（与用户截图红框一致）。

## 第 4 章骨架

- `4.1` 产品定义
- `4.2` 产品框架
- `4.3` 业务流程图
- `4.4` **端 / 系统名称**（如 WMS、OMS、APP 客户端、小程序、PC 网站）或 **功能点明细**（多端/多系统视图时）。第 4 章仅保留 `4.1`～`4.4`；容器树与功能点规格**全部写在 `### 4.4` 下**，不另设 `4.5`、`4.6` 或「导读 + 详述」双套目录。
- **单端/单系统视图**：4.4 = 端或系统名称；容器树自 4.4.1 起展开，层级数以实际原型结构为准。
- **多端/多系统视图**（详见主规则 §1.3.1）：4.4 = "功能点明细"；4.4.X = 各端/系统视图；**同名页面/菜单跨端不合并**，各端独立写完整 7 项用例规则。
- 若用户仅给出单端/单系统视图，不得自创第二端章节；容器树与功能点全部在该单视图下展开。
- 🚫 **绝无"通用"这个系统视图**：4.4.X 一律是原型真实系统（OMS / WMS / APP / 小程序 等），每个功能必归其真实所属系统下的菜单。**禁把功能提炼/归类成"通用"**——"通用"是 anno-server 解析不出系统时的兜底，属错误。AI 注入 zone / 首页 widget 等 fpKey 不含系统码的功能点时，**pin 必传 `pageKey="系统-页面"`**（如 OMS-home / WMS-recharge-list），系统码取 pageKey 首段；落"通用"会被机器闸 ⑱（`prd-system-attribution-gate.js`）判红、禁交付。

## 产品形态自适应规则（核心铁律）

**层级数量以实际原型为准，不固定层数**：

| 原型有几层容器 | PRD 就写几层 | 禁止行为 |
|---|---|---|
| B 端：系统 → 一级菜单 → 二级菜单 → 功能点 | 4 层完整写 | 强行合并或拆分菜单层 |
| B 端：系统 → 一级菜单 → 功能点（无二级） | 3 层，跳过二级 | 强行补一个空二级菜单 |
| APP：端 → Tab → 页面 → 功能点 | 4 层 | 把 Tab 和页面合并为一层 |
| APP：端 → 页面 → 功能点（无 Tab） | 3 层 | 强行加 Tab 层 |
| 小程序：端 → 页面 → 功能点 | 3 层 | 强行加区域层 |
| 任何产品：某页面 ≥ 8 个功能点 | 在页面下加"区域"层 | 忽略区域层导致功能点堆砌 |

**层级术语对照**（详见 prd-template-structure.mdc §1.3.2）：

| 层级位置 | B 端系统 | APP | 小程序 | H5/PC 网站 |
|---|---|---|---|---|
| `4.4.X` | 系统视图 | 端（APP客户端） | 端（小程序） | 端（PC/H5） |
| `4.4.X.Y` | 一级菜单 | Tab 模块 | 页面 | 顶部导航 |
| `4.4.X.Y.Z` | 二级菜单 | 页面 | 区域（可选） | 子页面 |
| `4.4.X.Y.Z.W` | 功能点 | 功能点 | 功能点 | 功能点 |

## `4.4` 以下业务树

- 自 `4.4`（系统）起，**每多一级菜单顺延一个小数段**，直至**路径末级菜单**（示例：`4.4.1` 设置 → `4.4.1.1` 业务设置 → `4.4.1.1.1` 自动开票）。  
- 菜单编号顺序必须严格跟随用户提供的菜单目录顺序，不得沿用历史编号顺序。
- **末级菜单的下一级**为**功能点**编号段。末级菜单与功能点**同名**时（如「自动开票」），仍占**两级**：上一级为菜单、下一级为功能点（`4.4.1.1.1` 菜单，`4.4.1.1.1.1` 功能点）。末级菜单与功能点**不同名**时（如末级「公司管理」、功能点「查询」），功能点紧跟末级菜单编号（`…1.1` 查询）。  
- 某一功能点编号记为 `…y`，其下固定四级子节：  
  - `…y.1` 位置  
  - `…y.2` 原型图  
  - `…y.3` 字段规范  
  - `…y.4` 用例规则  
  - 无字段规范时，`.3 字段规范` 节写「无」，用例规则固定落 `.4`（与「原型图」空值写「无」一致；编号不随有无字段规范前后挪动）

## Markdown 标题层级建议

- **菜单末级与功能点**均可用 `####` 连续展开；**功能点**下四级用 `#####`（`…y.1` 位置 等）。  

## 禁止冗余

不在 `### 4.4` 之上再叠一层仅重复的「菜单索引」标题；不将同一套 `4.4.*` 拆到 `4.6` 等平行章节。

## PRD 正文不写「元说明」

交付稿中，在 `####` 菜单/功能点标题下**不要**用 `•` 写「一级菜单」「二级菜单」「三级菜单」「功能点」「与下一节 x.x 区分」等解释性句子；**编号 + 标题**即层级。上述约定**仅**在本 rule 与 PRD SKILL / 提示词模板中约束作者与 AI。

## 菜单与功能点同名须「双写」`####`

当路径末级菜单名与下一级功能点名**完全一致**（如 `…1.1 自动开票` 为菜单、`…1.1.1 自动开票` 为功能点）时，PRD 目录中**必须保留两级 `####`**，**两节标题文字均完整写出**；**禁止**因与其它模块功能重名、或标题字面相同而**合并为一节、省略菜单级或省略功能点级**。区分以编号为准，不以「是否重名」删减标题。
