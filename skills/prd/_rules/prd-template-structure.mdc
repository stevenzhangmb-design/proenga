---
description: PRD 模板结构与写作铁律（章节、字段规范、用例规则七项、操作日志/提示消息/消息通知、二次确认、动作类规则、图片优先、请参考与完整不合并）
alwaysApply: true
---

# PRD 写作铁律（统一口径）

> 🏛 **本文件为 PRD 写作的单一真理源与最高权威。**
>
> 所属位置：`skills/prd/_rules/prd-template-structure.mdc` —— **宪法层**（与同目录 `prd-directory-numbering.mdc` 并列）。
>
> **权威性声明**：
> - SKILL.md / 提示词模板 / 示例文件 / 自检清单 / 标准块文件等如有不一致，**以本文件为准**
> - 任何下游文件（含 prd skill 内其他 .md 和 pm-design skill）禁止"创造"与本铁律冲突的规则
> - 修改本文件前请确保理解全部下游影响；建议改完同步检查 SKILL.md / quality-checklist.md 等是否同步

> 📌 **关于示例**：本文档中出现的具体业务示例（如「零售连锁系统-商品管理-商品库」「WMS-物流-平台物流」「OMS」等）**仅为说明规则用途**，不代表本铁律仅适用于零售 / 仓储 / 订单类系统。所有规则适用于**任意业务领域**的 PRD（ERP / SaaS / 营销 / 会员 / 财务 / IoT 等）；用户写 PRD 时按自己的真实业务命名替换示例即可。

---

## ⚠️ 顶级红色铁律（违反任一条 = 必须删除后重写）

### R-A 「图片识别 5 段绝不进 PRD 文件」铁律

**`图片绑定表` / `图片识别结果` / `图片归档清单` / `待确认项` / `合理化建议`** 五段属于 **AI 与用户的对话辅助输出**，**仅在对话消息中**呈现；**严禁**写入 PRD 正文文件（.md 归档稿）。

PRD 文件中**不得出现**以下任一形式：
- `### 本批截图识别摘要(...)` 或 `### 图片识别摘要(...)` 类标题
- `**图片绑定表**` 加粗段
- `**图片归档清单（固定五段）**` 加粗段（含"图编号 - 系统视图 - 菜单路径 - 功能点 - 图片用途"五段文本）
- `**待确认项**` 加粗段
- `**合理化建议**` 加粗段
- 任何把上述识别表/清单作为附录或独立节写进 PRD 的形式

PRD 文件中引用图片**只能**通过功能点的 `<功能点编号>.2 原型图` 写 `IMG-xx` 编号；识别细节由对话承载，不入档。

**判定**：PRD `.md` 文件出现以上任一形式即判为违规，须删除后重新输出。

### R-B 「PRD 固定 4 章 + 功能点直挂菜单」铁律

PRD 输出**固定 4 章**（1 开发目的 / 2 版本变更 / 3 术语 / 4 功能需求）；**严禁**另起 `## 5 功能点明细` 或任何其它平行章。功能点直接挂在第 4 章对应末级菜单（`##### 4.4.x.y`）下，标题层级为 `###### 4.4.x.y.z`。详见 §1.2。

### R-C 「字段规范仅收录数据字段」铁律

字段规范表内仅收录用户输入/选择/上传的数据控件；**严禁**收录操作按钮、操作链接、底部操作区、关闭图标等非数据元素。详见 §3.2。

### R-D 「PRD 须用户触发，AI 不得自行凭空撰写」铁律

AI **严禁**未经用户触发、自作主张撰写或"攒"出任何 PRD 性质内容（含 `.md`/`.docx` 文档、字段规范、用例规则七项、接口/字段清单等）。PRD 的产生**只能**由用户发起：

- **有原型**：用户在原型上**点选 / 框选**圈定功能点范围 → 在对话框说"为 XX 生成 PRD 标注" → AI 才按本铁律**真推理生成**（经 `anno-inject` 注入回原型）。圈选定位置、对话框填内容；AI 不得跳过圈选自行决定写哪些功能点。
- **无原型（PRD-only）**：用户明确提出场景 A（文字增量）或场景 B（截图驱动）并给出输入后，AI 才生成（见 SKILL.md 场景路由）。

**判定**：凡未经用户圈选 / 对话框触发即产出的 PRD 性质内容，一律违规，须删除。

> 注：竞品分析 / 功能盘点 / 改造方案等**非 PRD 文档**不受本条限制；但**不得**以"方案 / 规范"之名行 PRD 之实——一旦输出"字段规范 + 七项用例规则"即视为 PRD，必须走上述触发流程。

---

## 0 通用排版

- 项目符号统一为 Unicode `•`（U+2022）+ 半角空格；**禁止**在 `•` 前出现普通空格、制表符、NBSP（U+00A0）或其它空白；**禁止**用 `-` 作行首列表符、用形似圆点符号冒充 `•`。
- 全角冒号 `：` 用于标签结尾；半角冒号 `:` 仅用于英文/示意。
- Markdown 表格顶格写起。
- 列表项末尾保留两个半角空格（Markdown 换行）。

---

## 1 章节顺序

PRD **固定 4 章**，不得换序、不得新增第 5 章或其它平行章：

1. 开发目的：名称、建设目标、业务价值、核心问题、成功指标。
2. 版本变更：版本表必须完整。
3. 术语 / 图例定义：关键术语统一口径。
4. 功能需求：**含全部菜单结构与功能点明细**——产品定义、产品框架、业务流程图、系统视图与所有菜单/功能点均在本章内层级展开。
   - `4.3.x 系统视图边界`：**仅多系统视图必填**；单系统视图省略该节，业务边界并入 `4.3 业务流程图` 描述，不得为单系统视图凑出此节而造成正文臃肿。

**严禁**另起 `## 5 功能点明细` 章节。功能点直接挂接到第 4 章对应末级菜单下，「编号 + 标题即层级」。

### 1.1 第 4 章编号约定

详见同目录 [prd-directory-numbering.mdc](./prd-directory-numbering.mdc)（菜单逐级展开 / 功能点 .1 位置 .2 原型图 .3 字段规范 .4 用例规则 / 末级菜单与功能点同名时双写）。

### 1.2 章节标题层级（避免 TOC + 明细双层重复）

固定层级层叠（以单系统视图、4.4.1 为系统菜单、4.4.1.1 为末级菜单为例）：

| 层级 | 示例 | Markdown |
|---|---|---|
| 顶级章节 | `## 4 功能需求` | 2 # |
| 一级小节 | `### 4.1 产品定义` / `### 4.4 <系统视图>` | 3 # |
| 系统菜单 | `#### 4.4.1 <系统菜单名>` | 4 # |
| 末级菜单 | `##### 4.4.1.1 <末级菜单名>` | 5 # |
| 功能点 | `###### 4.4.1.1.1 <功能点名>` | 6 # |
| 功能点子项 | `4.4.1.1.1.1 位置` `.2 原型图` `.3 字段规范` `.4 用例规则` | 无 heading，普通行 |

**严禁结构**：
- 在第 4 章外另起 `## 5 功能点明细` 章节。
- 在不同位置重复同一菜单标题（如同时出现 `##### 4.4.1.1 商品库` 与 `### 4.4.1.1 零售连锁系统-商品-商品管理-商品库`）。
- 末级菜单只列 TOC、不写位置/原型图与功能点；末级菜单节内须**直接**写：
  1. `位置` 与 `原型图` 两段；
  2. 紧跟该菜单下全部功能点（`###### 4.4.x.y.z`）。
- **`位置` / `原型图` 段统一「两行式」（v1.0 强制 / 2026-06-08 用户固化）**：标签自成一行、**标签后不加「：」冒号**，内容写在下一行（markdown 用行尾两空格换行）——
  - `<编号> 位置`〔换行〕`<系统>-<一级菜单>-<二级菜单>-<末级菜单>：<功能名>`
    - 功能级 = 完整菜单路径 + `：` + 功能名（如 `WMS-财务-财务管理-充值管理：导出`）
    - 菜单级（末级菜单本身）= 仅写菜单路径、**不带**`：功能名`
    - 特殊入口（如"首页右上角余额卡"等非菜单导航项）照实描述其位置
  - `<编号> 原型图`〔换行〕`![IMG-xx 原型截图](screenshots/img-xx.png)`（或纯 `IMG-xx` 编号）
  - ❌ 废止旧式一行带冒号写法：`位置：…列表上方工具栏「X」按钮。` 、`原型图：…`
  - ⚠️ **各子项段（位置 / 原型图 / 字段规范 / 用例规则）之间必须空一行分隔**；尤其「位置」的内容行后要空行，否则 markdown 会把它与下一段「原型图」合并渲染成同一行（已知坑）。
- **禁止写 `• 操作清单：`**：该行枚举的功能点与紧随其下的 `###### 4.4.x.y.z` 标题完全重复，属冗余元说明，禁止入档。

> 提示：本 rule 文件自身的 `## 0 / ## 1 / ## 2 …` 编号是 rule 内部目录，与 PRD 输出章节无关。AI 生成 PRD 时只输出 1 / 2 / 3 / 4 共四章。

### 1.3 系统视图与菜单

- 系统视图名称、菜单名、功能点名一律取用户当次输入的真实命名，禁止套用「运营管理端 / 货主管理端 / 总部端 / 门店端」等示例占位。
- 多系统视图分端只在用户明确要求时输出；否则按单系统视图成文。
- **结构解析铁律**：用户以 `菜单路径：功能点1、功能点2…` 输入时，冒号前**仅**菜单路径，冒号后**仅**功能点；生成 PRD 时严格保留原始层级、顺序、命名，**禁止**合并/拆分/上提/下沉/改名/跨菜单挪动/重排序。若层级歧义先提最少必要问题，不得猜测。

#### 1.3.1 多系统视图 4 层结构（必读）

当输入含**多个系统视图**时（如 `WMS-物流-平台物流` + `OMS-物流-平台物流`），第 4 章按 **4 层结构**展开：

| 层级 | 含义 | 示例 |
|---|---|---|
| `#### 4.4.X` | **系统视图** | `4.4.1 WMS` / `4.4.2 OMS` |
| `##### 4.4.X.Y` | **一级菜单** | `4.4.1.1 物流` / `4.4.2.1 物流` |
| `###### 4.4.X.Y.Z` | **末级菜单** | `4.4.1.1.1 平台物流` / `4.4.2.1.1 平台物流` |
| `4.4.X.Y.Z.W` | **功能点**（无 heading 标识，普通行）| `4.4.1.1.1.1 启用` 等 |

**同名菜单跨系统不合并铁律**：

- 即使 WMS 和 OMS 都有「物流-平台物流」菜单，**必须各自独立写**完整的 7 项用例规则。
- ❌ 错误：把两系统的"查询/查看"合并到一处描述
- ✅ 正确：4.4.1 WMS 下写完整功能点，4.4.2 OMS 下另写完整功能点（即使功能点名相同）
- 理由：系统视图不同 → 权限边界、菜单路径、操作日志模块路径都不同

**§4.3 系统视图边界必填**（仅多系统视图）：

- 在 `### 4.3 业务流程图` 下加 `#### 4.3.2 系统视图边界` 段
- 列出每个系统的范围 + 跨系统数据/调用关系
- 示例：`WMS → 平台物流维护（启停）；数据源，供 OMS 引用 \| OMS → 平台物流查询/查看；引用 WMS 维护的数据`

**示例：用户输入**
```
1) WMS-物流-平台物流：启用、停用、查询、查看
2) OMS-物流-平台物流：查询、查看
```

**生成 PRD 第 4 章结构**：
```
4.4 功能点明细
  4.4.1 WMS
    4.4.1.1 物流
      4.4.1.1.1 平台物流
        4.4.1.1.1.1 启用
        4.4.1.1.1.2 停用
        4.4.1.1.1.3 查询
        4.4.1.1.1.4 查看
  4.4.2 OMS
    4.4.2.1 物流
      4.4.2.1.1 平台物流
        4.4.2.1.1.1 查询
        4.4.2.1.1.2 查看
```

→ **WMS 和 OMS 的"查询/查看"绝不合并**，各自独立完整。
- 同名功能在不同菜单下分别完整描述，不默认复用权限/字段/文案。

### 1.3.2 产品形态层级术语映射（通用铁律）

> 本节适用于**所有产品形态**：B 端系统（ERP / WMS / 零售门店 / SaaS）、APP、小程序、H5 / PC 网站。
> **核心不变量**：无论何种形态，功能点下的四板块（位置 / 原型图 / 字段规范 / 用例规则）与用例规则七节结构**完全一致**，只有"容器层级"的叫法随产品形态变化。

#### 产品形态 → 层级术语对照表

| 层级位置 | B 端系统（ERP/WMS/零售门店/SaaS） | APP | 小程序 | H5 / PC 网站 |
|---|---|---|---|---|
| `### 4.4` | 系统视图（WMS / OMS / 门店端） | 端（APP 客户端） | 端（小程序） | 端（PC 网站 / H5） |
| `#### 4.4.1` | 一级菜单（财务管理） | Tab 模块（首页 / 商品 / 购物车 / 我的） | 页面（商品详情页 / 购物车） | 顶部导航（商城 / 会员中心） |
| `##### 4.4.1.1` | 二级菜单（充值管理） | 页面（商品详情页 / 订单列表） | 区域（可选，见下） | 子页面（商品详情 / 结算） |
| `###### 4.4.1.1.1` | 功能点（充值 / 审核 / 导出） | 功能点（加入购物车 / 立即购买） | 功能点（加入购物车） | 功能点（搜索筛选 / 加购） |

**位置字段对应写法**（两行式，标签独占一行不加冒号）：

| 产品形态 | 位置写法示例 |
|---|---|
| B 端系统 | `WMS-财务管理-充值管理：充值` |
| APP | `APP客户端-商品Tab-商品详情页：加入购物车` |
| 小程序 | `小程序-商品详情页：加入购物车` |
| H5 / PC | `PC网站-商城-商品详情页：加入购物车` |

#### 层级取舍规则（避免强行凑层或漏层）

| 判断条件 | 处理方式 |
|---|---|
| APP / 小程序有底部 Tab 且 Tab 数 ≥ 3 | 保留 Tab 层（`#### 4.4.1`）作一级容器 |
| 小程序业务单一 / Tab ≤ 2 | 省略 Tab 层，直接 `#### 4.4.1` = 页面 |
| 一个页面功能点 ≥ 8 个 | 在页面下加"区域"层（`##### 4.4.1.1`），如 `底部操作区 / 规格选择区` |
| 一个页面功能点 < 8 个 | 省略区域层，页面直挂功能点 |
| B 端单系统视图且菜单只有一级 | `#### 4.4.1` = 末级菜单，功能点直挂，不强行加二级 |

#### 各形态 PRD 第 4 章结构示例

**B 端系统（WMS + OMS 双视图）**：
```
4.4 WMS 仓库端
  4.4.1 财务管理
    4.4.1.1 充值管理          ← 末级菜单
      4.4.1.1.1 查询
      4.4.1.1.2 充值
      4.4.1.1.3 审核
4.5 OMS 客户端
  4.5.1 财务管理
    4.5.1.1 充值管理
      4.5.1.1.1 查询
      4.5.1.1.2 充值
```

**电商 APP（底部 Tab 型）**：
```
4.4 APP 客户端
  4.4.1 首页 Tab
    4.4.1.1 首页
      4.4.1.1.1 搜索入口
      4.4.1.1.2 Banner 点击
  4.4.2 商品 Tab
    4.4.2.1 商品详情页
      4.4.2.1.1 规格选择
      4.4.2.1.2 加入购物车
      4.4.2.1.3 立即购买
      4.4.2.1.4 收藏
  4.4.3 购物车 Tab
    4.4.3.1 购物车页
      4.4.3.1.1 修改数量
      4.4.3.1.2 删除商品
      4.4.3.1.3 去结算
  4.4.4 我的 Tab
    4.4.4.1 个人中心页
      4.4.4.1.1 查看订单
      4.4.4.1.2 地址管理
```

**轻量小程序（单一场景，省 Tab 层）**：
```
4.4 小程序
  4.4.1 商品详情页             ← 直接从页面起，无 Tab 层
    4.4.1.1 规格选择
    4.4.1.2 加入购物车
    4.4.1.3 立即购买
  4.4.2 购物车页
    4.4.2.1 去结算
  4.4.3 结算页
    4.4.3.1 选择地址
    4.4.3.2 提交订单
```

**PC 网站 / H5（顶部导航型）**：
```
4.4 PC 网站
  4.4.1 商城导航
    4.4.1.1 商品列表页
      4.4.1.1.1 筛选搜索
      4.4.1.1.2 加入购物车（列表快速加购）
    4.4.1.2 商品详情页
      4.4.1.2.1 规格选择
      4.4.1.2.2 加入购物车
      4.4.1.2.3 立即购买
  4.4.2 会员中心导航
    4.4.2.1 我的订单页
      4.4.2.1.1 查看订单详情
      4.4.2.1.2 申请退款
```

#### 强制规则（与 §1.3.1 同级）

- **术语替换而非结构替换**：APP / 小程序 / 网站把"菜单"替换为"Tab 模块 / 页面 / 区域"，**章节编号规则、功能点四板块、用例规则七节均完全不变**。
- **跨形态同名功能不合并**：如 APP 端和小程序端都有"加入购物车"，必须各端独立写全，不得合并（理由：权限、入口路径、客户端行为可能不同）。
- **功能点命名取交互动词**：APP / 小程序 / 网站的功能点名称取用户操作动词（加入购物车 / 去结算 / 规格选择 / 收藏），不用 B 端的"新增 / 编辑 / 审核"套话。
- **位置字段照实描述**：无传统菜单路径时，按`端-页面-区域（可选）：功能点名`格式写，禁止留空或写"N/A"。

### 1.4 版本变更表标准（PRD §2 章节）

**固定 4 列结构**（一字不易）：

| 列名 | 含义 | 填写规范 |
|---|---|---|
| 版本 | 版本号 | `vX.Y` 格式（X=大版本，Y=小迭代，如 v1.0、v1.1、v2.0）|
| 日期 | 变更日期 | `YYYY-MM-DD` 格式 |
| 变更人 | 提交变更的人 | 真实姓名或登录账号；多人协作时列首要负责人 |
| 变更内容 | 本版本变更点 | 业务化描述，每条独立短句；多条用 `；` 分隔；含改动模块名 |

**表头模板**：
```markdown
| 版本 | 日期 | 变更人 | 变更内容 |
|---|---|---|---|
| v1.0 | 2026-04-30 | 张三 | 首次发版：覆盖商品库、门店商品、商品分类等 8 个模块 |
| v1.1 | 2026-05-10 | 李四 | 商品库：新增"发布到门店"功能点；门店商品：优化改价弹窗 |
```

**变更内容描述规则**：

- ✅ 含具体模块/功能点名（如 `商品库：新增"发布到门店"`）
- ✅ 多条用 `；` 分隔
- ✅ 用动词领起（新增 / 优化 / 删除 / 修复 / 调整）
- ❌ 禁止泛化（如「优化体验」「修改了一些字段」）
- ❌ 禁止空白行或仅写"初版"

**版本号约定**：

| 变化级别 | 升版规则 | 示例 |
|---|---|---|
| 首次发版 | `v1.0` | - |
| 小迭代（字段/文案/局部调整）| 末位 +1（如 v1.0 → v1.1）| - |
| 模块级新增 / 重大变更 | 中位 +1（如 v1.1 → v1.2 或 v2.0）| - |
| 重写 / 架构级重构 | 首位 +1（如 v1.x → v2.0）| - |

### 1.5 术语 / 图例定义章节标配（PRD §3 章节）

**必须定义的术语**（按 PRD 涉及的动作类型出现就必出现）：

| 术语 | 触发出现条件 | 标准定义 |
|---|---|---|
| **软删除** | PRD 含「删除」功能点 | 数据从列表移除但不物理删除；标记 `is_deleted=true`；保留可追溯；不可恢复时另注明 |
| **唯一性校验** | PRD 含「同 X 下唯一」约束 | 在指定业务范围内（如同货主下、同分类下）唯一，新增/编辑时校验 |
| **状态机** | PRD 含对象状态流转（如商品「草稿→已发布」）| 业务对象在生命周期内可能的状态及触发跳转的动作集合 |
| **二次确认弹窗** | PRD 含删除/启用/停用/上下架/改价/发布 等动作 | 用户点击不可逆操作按钮后弹出的"确认/取消"弹窗，防误操作 |
| **异步任务** | PRD 含导入/导出/批量操作/发布到门店 等可能耗时操作 | 用户提交后系统进入后台处理，处理中可关闭窗口，完成后通过站内信通知 |
| **双消息** | PRD 含发布到门店/上架/下架/改价/订单流转 等业务下发类 | 一个动作产生 2 条独立消息：操作人确认 + 受影响方通知（详见 §4.6） |
| **回显** | PRD 含编辑/复制功能点 | 进入编辑/复制弹窗或页面时，自动从数据库读取已有数据填入对应字段 |
| **条件必填** | PRD 含「某字段在 X 条件下必填」 | 字段是否必填取决于其它字段的值（如保质期管理=开启时，保质期单位必填）|

**禁止行为**：

- ❌ 在功能点正文里重复抄写术语全文（应用 `（详见术语：<术语名>）` 引用）
- ❌ 多个术语写法不一致（如同一份 PRD 中"软删除"和"逻辑删除"混用）
- ❌ 涉及术语却不在 §3 定义就直接使用（如写"双消息"但 §3 无定义）

**术语章节写法模板**：

```markdown
## 3 术语 / 图例定义

| 术语 | 定义 |
|---|---|
| 软删除 | 数据从列表移除但不物理删除；标记 `is_deleted=true`；保留可追溯。 |
| 唯一性校验 | 在指定业务范围内（同货主下、同分类下等）唯一，新增/编辑时校验。 |
| ... | ... |
```

### 1.6 「待确认项」标准化（对话辅助输出，不进 PRD 文件）

**性质 / 位置**：

- 与图片绑定表、识别结果、归档清单、合理化建议并列，属 **AI 与用户的对话辅助输出**
- **严禁写入 PRD 正文文件**（.md 归档稿）
- 输出位置：AI 对话消息中，PRD 正文之前或之后独立段，标题为 `## 待确认项`

**统一 5 列结构**（一字不易）：

| 编号 | 问题类型 | 涉及范围 | AI 当前处理 | 待用户确认 |
|---|---|---|---|---|
| Q-01 | <类型> | <功能点 / 字段 / 菜单等具体定位> | <当前默认或暂处理方式> | <待用户回答的问题或可选项> |

**5 大触发场景（出现任一**必须**进待确认项）**：

| 场景 | 典型问题 | 应填问题类型 |
|---|---|---|
| **图片相关**（详见 image-recognition §6）| 图片间冲突 / 图文冲突 / 图片模糊 / 控件类型歧义 | `图片冲突` / `图片模糊` / `控件类型歧义` |
| **业务规则缺失** | 用户未提供唯一性范围/字段权限/状态流转规则/业务联动约束 等 | `业务规则缺失` |
| **菜单/功能点不匹配** | 用户给的菜单路径与 PRD 结构不一致 / 拼写错误 / 菜单不存在 | `菜单不匹配` |
| **模板 / 库不命中** | 字段语义不在 §3.1.4 行业标准库 / 默认值不在 §3.1.5 业务惯例库 / 消息通知不在 §4.6.5 35 类模板 | `模板/库不命中（§<章节号>）` |
| **多种合理解读** | 同一字段或动作有多种解读（如"创建人"是查询条件 vs 只读显示）| `多重解读` |

**AI 行为铁律**：

- ❌ **禁臆测**：信息缺失/模糊不得用常识/经验/同类项目默认值补全
- ❌ **禁推托**：禁止写 `以实现为准` / `按业务定` 等推托语（已在 §3.1.4 / §3.1.5 禁止）
- ❌ **禁自创**：消息通知模板不在 35 类时不得自创，必须进待确认
- ✅ **必进项**：出现 5 大触发场景任一 → **必须**在对话输出待确认项
- ✅ **AI 当前处理透明**：必须显式说明 AI 暂时填了什么，方便用户判断是否接受
- ✅ **逐项编号**：Q-01、Q-02 顺序，方便闭环追踪

**闭环规则**：

- 用户答复后，AI 必须**更新 PRD 正文** + **从待确认项中移除该项**
- 用户长期未答复（>3 天或评审节点临近）→ 标 `保留（未答复）`，并注明影响范围
- **评审时所有待确认项必须澄清后才能签字归档**——quality-checklist 自检项 53 已要求"留有待确认问题供评审闭环"

**示例输出**：

```markdown
## 待确认项

| 编号 | 问题类型 | 涉及范围 | AI 当前处理 | 待用户确认 |
|---|---|---|---|---|
| Q-01 | 业务规则缺失 | 4.4.1.1 商品库-新增 / 字段「商品编号」 | 按"同货主下唯一"暂填约束 | 唯一性范围是「同货主下唯一」还是「全局唯一」？ |
| Q-02 | 图片模糊 | 字段「成本价」/ IMG-02 | 暂按"非必填"处理 | 该字段是否必填？是否有红 * 标记？ |
| Q-03 | 模板/库不命中（§4.6.5）| 功能点「自动补货」消息通知 | 暂不输出消息通知模板 | 「自动补货」属业务下发类还是异常告警类？请补充自定义模板或从 35 类选最贴近的 |
| Q-04 | 图片冲突 | 字段「商品状态」/ IMG-01（5 枚举）vs IMG-03（6 枚举）| 按 IMG-03 的 6 枚举暂处理 | 最终枚举值是 5 个还是 6 个？哪张图是最新版？ |

上述 4 项需答复后才能定稿。答复后我会更新 PRD 正文并从待确认项中移除。
```

---

## 2 业务流程图

**绘制约束（AI 内部遵循，不写入 PRD 正文）**：

- 主流程图、子流程图均按泳道角色绘制；先定义泳道角色清单。
- 每个场景含开始与结束节点；判断节点写清条件（通过/驳回、成功/失败等）。
- 子流程按场景拆分（新增、编辑、审核、异常处理 等），每图描述一个场景主线。
- 命名：`主流程-<模块名>` / `子流程-<模块名>-<场景名>`。
- 多系统视图时分开描述，不混写。

**PRD 正文输出铁律**：

`### 4.3 业务流程图` 章节正文**仅输出 mermaid 流程图本身**，外加该流程下确实需要承载的业务边界口径（如"数据边界""跨菜单联动"等）。**禁止**在正文中写以下"元说明"段落：

- ❌ `• 主流程图：主流程-<模块名>（泳道：...）`
- ❌ `• 子流程图：子流程-<模块名>-<场景名>；...`
- ❌ `• 泳道角色清单：...`
- ❌ `• 绘制规范：...` 及其下的"每个泳道仅放本角色动作""判断节点写清条件"等子条款

理由：流程图本身已展示流程名、泳道、节点、条件——重复用文字列出这些元数据是"元说明"，对读者无信息价值，徒增臃肿。绘制规范是 AI 写作期的内部约束（上文已列），不该入档。

**允许保留的辅助文字**：

- 业务边界口径（如"商品库为总部主数据；门店商品为门店可售视图"）
- 跨菜单联动规则（如"仓库停用级联停用库区"）
- 子流程引用（若存在多张 mermaid 子图时，在主图后另插 `**子流程-<场景>**` 标题与对应 mermaid，但禁止只写名称不附图）

### 2.1 mermaid 流程图标准化模板

**mermaid 类型选择**：

| 流程类型 | 选用 | 理由 |
|---|---|---|
| 主流程 / 子流程（含泳道角色） | `flowchart TB`（竖向，泳道纵向堆叠）或 `sequenceDiagram` | 竖向才能塞进竖版 PRD/Word 页面、字够大 |
| 简单线性流程（无分支）| `flowchart TB` | 竖向铺满页宽不被压扁 |
| 状态机（对象状态流转）| `stateDiagram-v2` | 状态切换清晰 |
| 时序交互（跨系统调用）| `sequenceDiagram` | 时序明确 |

> **流程图方向铁律（竖向优先）**：PRD/Word 是竖版页面。横向 `LR` 一排节点多了会变超宽（>2.5:1），渲染进文档铺满页宽后高度极小、字被压成 2pt 看不清。因此**业务流程图一律用竖向 `TB`/`TD`**（泳道用 `subgraph` 纵向堆叠），禁止横向 `LR`。anno-server 渲染时会按真实宽高比自动铺满页面、字号 22px，但若源码画成超宽 LR 仍救不回来——**源头必须竖向**。资产闸会检查流程图 PNG 宽高比 >2.5:1 直接报红。

**节点形状约定**（flowchart）：

| 节点类型 | 形状 | mermaid 语法 |
|---|---|---|
| 开始 | 圆角矩形 | `A([开始])` |
| 结束 | 圆角矩形 | `Z([结束])` |
| 动作 / 步骤 | 矩形 | `B[用户填写表单]` |
| 判断 | 菱形 | `C{是否通过审核?}` |
| 数据 / 文档 | 平行四边形 | `D[/导出文件/]` |
| 子流程引用 | 双框矩形 | `E[[子流程-改价格]]` |

**泳道角色标注**（subgraph 模拟泳道）：

```mermaid
flowchart TB
    subgraph 总部商品运营
        A([开始]) --> B[新增商品]
        B --> C{审核}
    end
    subgraph 商品审核员
        C -->|通过| D[发布到门店]
        C -->|驳回| E[修改]
    end
    subgraph 门店店长
        D --> F[上架]
    end
    F --> Z([结束])
```

**连线约定**：

| 连线类型 | mermaid 语法 | 用途 |
|---|---|---|
| 普通流转 | `A --> B` | 默认 |
| 带条件 | `A -->|通过| B` | 判断分支 |
| 反向回退 | `A -.-> B` | 异常 / 驳回 |
| 强依赖 | `A ==> B` | 强约束流转 |

**禁止写法**：

- ❌ 仅写文字描述（如「先 X 后 Y 再 Z」）不附 mermaid → 必须画图
- ❌ 元说明段（已在 §2 禁止）
- ❌ 横向 `flowchart LR` / 超宽布局（PNG 宽高比 >2.5:1）→ 文档里被压成小图字看不清，**必须改竖向 `TB`**（资产闸会报红）
- ❌ 单图超过 20 个节点 → 拆子流程
- ❌ 节点文字超过 12 字 → 简化或拆步

**主流程模板**（CRUD 类业务）：

```mermaid
flowchart TB
    subgraph 用户
        A([开始]) --> B[填写数据]
        B --> C[提交]
    end
    subgraph 系统
        C --> D{校验}
        D -->|通过| E[保存数据]
        D -->|失败| F[返回错误提示]
        E --> G[列表刷新]
    end
    G --> Z([结束])
    F -.-> B
```

**子流程模板**（流程审批类）：

```mermaid
flowchart TB
    subgraph 提交人
        A([开始]) --> B[提交审核]
    end
    subgraph 审核人
        B --> C{审核}
        C -->|通过| D[变更状态: 已通过]
        C -->|驳回| E[变更状态: 已驳回]
    end
    subgraph 提交人
        E -.-> F[修改后重新提交]
        F --> B
    end
    D --> Z([结束])
```

---

## 3 字段规范

### 3.1 表格结构

- **五列表头固定**：`字段名称` `类型` `是否必填/必选` `默认值` `约束规则`。
- 表头模板：`| 字段名称 | 类型 | 是否必填/必选 | 默认值 | 约束规则 |`
- 分隔行：`| --- | --- | --- | --- | --- |`
- **一字段一行**；禁止以 `/` 把多个字段合并为一行。
- 字段级错误文案不进字段规范表，统一进 `提示消息` 小节。
- **无字段规范铁律**：**仅**纯操作类功能点（删除 / 导出 / 启停 / 打印 / 复制 等，**既无数据录入控件、也无展示数据**）→ `.3 字段规范` 节**写「无」**（与「原型图」空值写「无」一致），**不省略、不跳节**；编号固定 `.3 字段规范`、`.4 用例规则`，不随有无字段规范前后挪动。
- **展示/看板/widget/查看 类不准简单写「无」铁律**：功能点的原型区域**有展示数据**（如首页 widget 的 账户余额/充值总额/数量/状态/时间、看板统计、详情只读字段等）→ **必须以原型实际展示字段写成只读字段规范**（类型=只读金额/只读数值/只读文本/只读占比 等，必填=否，**默认值=回显**，约束规则写展示口径），**禁因"没有输入表单"就简单写「无」**。判定：能从原型该区域读出在展示的数据项，就不是"纯操作类"，必须逐项落字段规范。（2026-06-28 补：首页 4 个 widget 曾被误判为「无」，根因=把"展示类"误套"纯操作类"。）

#### 3.1.1 截图 → 表格 五列填写映射铁律

**字段名称**：界面标签精确文字，不改写不缩写（如 `供应商名称` / `省 / 市 / 区`）。

**类型**：按截图 UI 控件填，**采用业务化命名**，必要时加括号补充功能。

| 截图 UI | 类型列写法 |
|---|---|
| 单行输入框 | `文本输入框` |
| 多行 textarea | `文本输入框`（或 `文本域` 当业务需要区分时）|
| 富文本编辑器 | `富文本编辑器` |
| 下拉选择 ▼ | `下拉选择` |
| 下拉旁带「新建 X」「刷新」等链接 | `下拉选择（支持新建 X）` |
| 下拉多选 | `下拉选择（多选）` |
| 单选 radio | `单选` |
| 单选 radio 组（多选项横排）| `单选按钮组` |
| 选项卡式按钮组 | `选项卡按钮组` |
| 复选框单个 | `复选框` |
| 复选框组（多选）| `复选框组` |
| 级联 N 级 | `级联选择（X 级）` 或具体 `级联选择（省市区三级）` |
| 日期单选 | `日期选择器` |
| 日期时间 | `日期时间选择器` |
| 日期范围 | `日期范围选择器` |
| 数字输入 | `数值输入框` |
| 金额输入带币种 | `金额输入框（带币种）` |
| 图片/文件上传 | `上传（图片，最多 N 张）` / `上传（文件）` |
| 开关 toggle | `开关` |
| 表格 | `表格` |
| 树形 | `树形选择` |
| 标签 | `标签（多值）` |

**是否必填/必选**：仅取 `是` / `否` / `条件必填`（其它写法违规）。**严格以截图视觉信号为准，不做业务推断**。

| 截图特征 | 填写 |
|---|---|
| 字段名前红 `*` | `是` |
| 无红 `*` | `否` |
| 业务条件触发必填（截图明示如"开启保质期"时显示红 *）| `条件必填`（并在 `约束规则` 列写清触发条件）|

**禁止行为（不做业务推断）**：

- ❌ "业务主键应该必填" → 截图无 * 仍写 `是`
- ❌ "地址 / 联系方式业务上必填" → 截图无 * 仍写 `是`
- ❌ "重要字段默认必填" → 任何无视觉信号的推断
- ✅ **正确做法**：截图有 * 就 `是`；截图无 * 就 `否`；业务上有不同诉求时**用户后续提示更正**。

**约束规则连带规则**：

- 当 `必填=否` 时，约束规则**不应**含 `不能为空` 字样（因为允许为空）。
- 当 `必填=是` 时，约束规则**可以**含 `不能为空` 作为强化（如 `不能为空；长度建议 1~100 字符`）。
- 行业标准格式约束（如 `只能包含英文、数字、下划线`、`符合 EAN-13 标准`）与必填**无关**——即使必填=否，只要有值就要符合格式。

**默认值**：**关键铁律（2026-06-28 重定义，详见 §3.1.5）——默认值列 = "字段打开时默认显示什么"，一律以原型为准**。无默认值就写占位原文，有默认值就写默认值。

| 截图特征 | 默认值列写法 | 含义 |
|---|---|---|
| 输入框灰字占位 `请输入xxx` | `请输入xxx`（一字照原型）| 无默认值 + UI 占位提示 |
| 下拉灰字占位 `请选择` | `请选择` | 无默认值 + 占位文案 |
| 下拉灰字占位 `请选择XXX` | `请选择XXX` | 同上，含具体文案 |
| 级联灰字占位 `请选择省 / 市 / 区` | `请选择省/市/区` | 无默认值 + 具体占位 |
| 输入框/选择框无占位、纯空 | `空` 或 `无` | 无默认值 + 无占位 |
| 上传控件（无占位）| `无` | 无默认值 + 无占位 |
| 只读字段随主数据带出 | 默认显示值，如 `-` | 默认显示真实值 |
| 下拉黑字显示业务值 `无`（非灰字）| `"无"`（带引号）| 「无」是真实业务值（如"分类=无"），是默认值不是占位 |
| 系统配置默认（如默认选中"启用"）| 直接写值 `已启用` | 业务有默认值 |
| 单选默认选中（如 ●无物流发货）| 直接写选中项 `无物流发货` | 业务有默认值 |
| 默认勾选某复选框 | `已勾选` | 业务默认勾选 |
| 默认未勾选 | `未勾选` | 业务默认未勾选 |
| 编辑 / 查看场景 | `回显` | 数据回填 |

**核心区分**：
- 灰字占位（placeholder）= 无默认值 → **写占位原文（一字以原型为准）**，无占位才写 `空`/`无`（§3.1.5，**禁 `空（显示"X"）` 旧包装**）
- 黑字实际值（actual value）= 有默认值 → 直接写或带引号
- 占位提示**本身不是业务默认值**，但无默认值时默认值列照原型填占位原文。

**约束规则**：业务化、可读、可测试的语言；多条用 `；` 分隔。

| 字段类型 / 业务特征 | 约束规则典型写法 |
|---|---|
| 必填文本 | `不能为空；长度建议 X~Y 字符`（按字段长度业务定）|
| 业务编码（X 编码 / 编号）| `只能包含英文、数字、下划线；不可重复；长度建议 X~Y 字符` |
| 业务名称（X 名称）| `不能为空；长度建议 1~100 字符`（默认）|
| 短地址 / 标题 | `长度建议 1~50 字符` |
| 长地址 / 备注 / 描述 | `长度建议 1~200 字符`（地址）/ `1~500 字符`（备注） |
| 下拉枚举 | `从预设的 X 选项中选择` |
| 下拉支持新建 | `可从已有 X 中选择，或通过"新建 X"添加` |
| 级联选择 | `必须选择到具体的 X 级别` |
| 数值类 | `非负；保留 X 位小数；范围 X~Y` |
| 金额类 | `非负；保留 2 位小数；按币种配置（如 BRL）` |
| 日期范围 | `开始时间 ≤ 结束时间` |
| 上传类 | `支持格式 jpg/png，单文件 ≤ 5MB，最多 N 张` |
| 唯一性 | `不可重复` / `同 X 范围内不可重复` |
| 安全 | `防 SQL 注入 / XSS` |
| 关联业务下方有灰字说明 | 在约束规则末尾追加 `；<说明文字>` |

#### 3.1.2 兜底长度约束（截图看不到时按字段名分类）

业务长度限制通常截图不显示，按字段名兜底：

| 字段名含义 | 长度兜底 |
|---|---|
| 编码 / 编号 / ID | 1~50 字符 |
| 名称 / 标题 | 1~100 字符 |
| 短地址 / 简介 | 1~200 字符 |
| 备注 / 描述 / 说明 | 1~500 字符 |
| 长文本 / 详情 | 1~2000 字符 |
| 手机号 | 11 位 |
| 邮箱 | 标准邮箱格式，长度 1~100 字符 |
| URL | 1~500 字符 |

业务有特殊要求时按用户指示覆盖兜底。

#### 3.1.4 约束规则的推导铁律（按 UI 控件类型分流）

**核心分流**：

| UI 控件类型 | 约束规则来源 |
|---|---|
| **输入类**（文本输入框 / 文本域 / 数值输入框 / 金额输入框 / 日期选择器 / 日期范围 / 上传 等用户键入或上传） | **基于行业 / 领域标准规范推导**——按下表「行业标准约束库」自动套用，截图未明示也照写 |
| **选择类**（下拉选择 / 单选 / 单选按钮组 / 复选框 / 复选框组 / 选项卡按钮组 / 级联选择 / 树形选择 等用户选择） | **以截图实际选项为准**——不套行业模板，写法见下方「选择类约束写法」 |

##### 输入类（按行业标准推导）

AI 看到输入类字段后，须根据字段语义自动套用业内通用约束，不要写"按业务定"等推托语。

**常见行业标准约束库**（按字段语义自动套用）：

| 字段语义 | 行业标准约束 |
|---|---|
| 手机号 | `11 位纯数字；以 1 开头；第 2 位为 3~9` |
| 座机号 | `区号 3~4 位 + 号码 7~8 位；含可选分机号` |
| 邮箱 | `符合 RFC 5322 格式（xxx@xxx.xxx）；长度 1~100 字符` |
| 身份证号 | `18 位；前 17 位纯数字、末位为数字或 X；含出生年月日校验、地区码校验、校验位 Luhn 校验` |
| 统一社会信用代码 | `18 位；由数字与大写字母组成（不含 I/O/Z/S/V）；含校验位` |
| 银行卡号 | `16~19 位纯数字；Luhn 算法校验` |
| 车牌号 | `符合中国车牌标准格式（含新能源车牌 8 位）` |
| 邮政编码 | `6 位纯数字` |
| 金额 | `非负；保留 2 位小数；按币种配置（如 BRL/CNY 默认 2 位、JPY 0 位）` |
| 价格 | `非负；保留 2 位小数；不超过业务上限（如 9 位整数）` |
| 百分比 | `0~100；最多保留 2 位小数` |
| 库存数量 | `非负整数；上限 999999`（按业务调整） |
| 重量 | `非负；保留 2~4 位小数；按单位换算` |
| 长度 / 尺寸 | `非负；保留 1~2 位小数` |
| 税率 | `0~100%；保留 2 位小数` |
| URL | `以 http:// 或 https:// 开头；长度 1~500 字符` |
| IP 地址 | `IPv4 标准格式（4 段 0~255）或 IPv6 标准格式` |
| 密码 | `8~20 位；至少含大写字母、小写字母、数字、特殊字符各一` |
| 用户名 | `4~20 位；字母+数字+下划线；首字母为字母` |
| 日期 | `YYYY-MM-DD 格式` |
| 时间 | `HH:mm:ss 格式` |
| 日期范围 | `开始时间 ≤ 结束时间` |
| 商品条形码（EAN/UPC/GTIN）| `符合 EAN-13（13 位）/ EAN-8（8 位）/ UPC-A（12 位）/ GTIN-14（14 位）标准；末位为校验位` |
| 商品 SKU / 编码 | `只能包含英文、数字、横线、下划线；不可重复；长度 1~50 字符` |
| 颜色值 | `符合 HEX（#RRGGBB）或 RGB 格式` |
| 经纬度 | `经度 -180~180；纬度 -90~90；保留 6 位小数` |
| 微信号 | `6~20 位；字母开头；字母+数字+下划线+减号` |
| QQ 号 | `5~12 位纯数字；首位非 0` |

**ERP / 财务类**：

| 字段语义 | 行业标准约束 |
|---|---|
| 凭证号 | `符合 "<前缀>-<YYYY>-<序号>" 格式（如 JR-2026-0001）；同账期内唯一` |
| 会计科目编码 | `4~10 位纯数字；树形结构（按级别分段）；同公司内唯一` |
| NCM 编码（巴西商品分类）| `8 位纯数字；按巴西海关 NCM 标准；含校验位` |
| CEST 编码（巴西州际） | `7 位纯数字（XX.XXX.XX 格式）；按巴西州际商品代码` |
| CFOP 编码（巴西操作） | `4 位纯数字（首位 1~7）；按巴西税务出/入操作分类` |
| HSN 编码（印度商品） | `4~8 位纯数字；按印度商品税分类` |
| GST 号（印度税号） | `15 位字母+数字；以 2 位州代码开头；含校验位` |
| 增值税号 / 纳税人识别号 | `按所在国规范（如 EU VAT 7~14 位、CN 纳税人识别号 15~20 位）` |
| 发票号 | `按发票类型与国家规范（如 CN 增值税发票 10 位纯数字）；全局唯一` |
| 客户编码 / 供应商编码 / 货主编码 | `字母+数字+横线；长度 4~20；全局唯一` |
| 部门编码 / 员工编码 / 组织编码 | `字母+数字；长度 4~20；同公司内唯一` |
| 项目编码 / 任务编码 | `字母+数字+横线；长度 4~30；同公司内唯一` |

**WMS / 仓储类**：

| 字段语义 | 行业标准约束 |
|---|---|
| 库位编码 | `分层结构（区-排-架-层-位），如 A-01-03-02-1；同仓库内唯一` |
| 仓库编码 | `字母+数字；长度 2~10；全局唯一` |
| 批次号 | `符合 "<日期 YYYYMMDD>-<序号>" 格式；同 SKU 下唯一` |
| 序列号 SN | `字母+数字；长度 6~30；全局唯一` |
| 托盘号 | `符合 "<前缀 PT>-<序号>" 格式；全局唯一` |
| 运单号 | `各物流公司规范不一；通常 10~25 位数字+字母` |
| 入库单号 / 出库单号 / 调拨单号 | `符合 "<前缀>-<YYYYMMDD>-<序号>" 格式（如 IN-20260525-0001）；同类型同日内唯一` |
| ASN 号 | `符合 "ASN-<日期>-<序号>" 格式；全局唯一` |
| 盘点单号 / 拣货单号 / 复核单号 | `符合 "<前缀>-<YYYYMMDD>-<序号>" 格式；同类型同日内唯一` |

**POS / 零售收银类**：

| 字段语义 | 行业标准约束 |
|---|---|
| 小票号 | `符合 "<终端号>-<YYYYMMDD>-<序号>" 格式；终端内唯一` |
| 交易流水号 | `数字串；长度 14~20；全局唯一` |
| 收银机编号 / POS 终端号 | `符合 "<前缀 POS>-<门店编号>-<序号>" 格式；全局唯一` |
| 班次号 | `符合 "<门店编号>-<YYYYMMDD>-<序号>" 格式；门店内唯一` |
| 第三方支付流水号 | `各支付平台规范不一；通常 20~32 位字母+数字` |
| 门店编号 | `字母+数字；长度 2~10；全局唯一` |

**商城 / 订单类**：

| 字段语义 | 行业标准约束 |
|---|---|
| 订单号 | `字母+数字；长度 16~25；全局唯一` |
| 退货单号 / 售后单号 | `符合 "<前缀 RF/AS>-<原订单号>" 格式；全局唯一` |
| 物流单号 / 快递单号 | `各物流公司规范不一；通常 10~25 位数字+字母` |
| 优惠券码 | `字母+数字（大写）；长度 6~20；全局唯一` |
| 评价星级 | `1~5 整数` |
| 会员号 / 会员卡号 | `字母+数字；长度 6~20；全局唯一` |
| 积分余额 | `非负整数；上限按业务配置` |
| 储值余额 | `非负；保留 2 位小数；按币种配置` |

**数据格式 / 国际标准类**：

| 字段语义 | 行业标准约束 |
|---|---|
| 货币代码 | `符合 ISO 4217 标准（3 字母大写，如 CNY/USD/BRL）` |
| 国家代码 | `符合 ISO 3166-1 alpha-2（2 字母）或 alpha-3（3 字母）` |
| 语言代码 | `符合 ISO 639-1（2 字母小写，如 zh/en）` |
| 时区 | `符合 IANA Time Zone 数据库（如 Asia/Shanghai、America/Sao_Paulo）` |
| MAC 地址 | `符合 XX:XX:XX:XX:XX:XX 格式（6 段十六进制）` |
| UUID | `符合 RFC 4122 标准（8-4-4-4-12 共 36 位含连字符）` |

**推导原则**：

1. 字段语义命中上表 → 直接套用对应标准。
2. 字段语义为标准的变体（如"联系电话"=手机号/座机号二选一）→ 同时列出两套约束。
3. 字段语义不命中上表 → 按 §3.1.2 兜底长度约束 + 业务化描述。
4. 截图明示自定义约束（如"长度 1~50 字符"红字标注）→ 以截图为准，覆盖行业标准。
5. 用户在 prompt 中明示自定义约束 → 以用户指示为准。

**输入类写法示例**：

| 字段 | 推导后约束规则 |
|---|---|
| 联系人手机 | `11 位纯数字；以 1 开头；第 2 位为 3~9；不能为空` |
| 客户邮箱 | `符合 RFC 5322 格式；长度 1~100 字符` |
| 商品条形码 | `符合 EAN-13 / UPC-A / GTIN-14 标准；末位为校验位` |
| 销售单价 | `非负；保留 2 位小数；按币种配置；不能为空` |
| 收货地址 | `不能为空；长度建议 1~200 字符` |
| 法人身份证 | `18 位；含出生年月日校验、地区码校验、Luhn 校验位` |

##### 选择类（以截图实际选项为准）

AI 看到选择类字段后，须**从截图提取真实可选项**作为约束规则，**禁止**套用行业模板（因为选项是业务定义的，不存在通用标准）。

**写法模板**：

| 截图特征 | 约束规则写法 |
|---|---|
| 单选 / 单选按钮组 / 选项卡按钮组 + 截图可见选项 | `从"<选项1>/<选项2>/...选项 N>"中选择` 或直接列举 `<选项1>、<选项2>、...` |
| 下拉选择 + 截图可见选项 | 同上 |
| 下拉选择 + 截图仅显示占位（看不到选项）| `从预设的<X>选项中选择`（需用户补充具体枚举值时进「待确认项」）|
| 下拉旁带「新建 X」「刷新」等链接 | `可从已有<X>中选择，或通过"新建<X>"添加` |
| 下拉支持多选 | `多选；从预设选项中选择` |
| 复选框 / 复选框组 + 多选项 | `多选；至少选一项` 或 `多选；从"<选项1>、<选项2>、..."中选择` 或具体业务规则 |
| 级联选择 N 级 | `必须选择到<最低层级>级别`（如 `必须选择到具体的区/县级别`）|
| 树形选择 | `从树形结构选择；可选叶节点 / 非叶节点（按业务定）` |

**选择类写法示例**：

| 字段（截图特征） | 约束规则 |
|---|---|
| 商品类型（选项卡按钮组，可见"实物商品/虚拟商品"）| `从"实物商品/虚拟商品"中选择` |
| 销售渠道（复选框组，"线上销售/线下销售"）| `多选；至少选一项；从"线上销售、线下销售"中选择` |
| 物流模式（下拉，仅显示"请选择"占位）| `从预设的物流模式选项中选择` |
| 物流模式（下拉，可见"仓配/直送/直通"）| `从"仓配/直送/直通"中选择` |
| 供应商分类（下拉，旁带"新建分类\|刷新"）| `可从已有分类中选择，或通过"新建分类"添加` |
| 省/市/区（级联三级）| `必须选择到具体的区/县级别` |
| 商品分类（树形）| `从商品分类树选择；仅可选叶节点（末级分类）` |

**禁止写法（选择类）**：

- ❌ 把行业标准套到选择类上（如把"手机号"行业标准套到下拉选择的"联系方式类型"字段）
- ❌ 选择类约束写"格式校验"（选项是枚举，不存在格式校验）
- ❌ 选项截图清晰却写"按业务定" / "按预设选项"（应直接列举）

##### 约束规则禁写内容（任何字段都不要写）

| 禁写内容 | 应该放哪 |
|---|---|
| 通用安全约束（`防 SQL 注入`、`防 XSS`、`防注入` 等）| PRD 顶部「非功能要求」段统一声明，**禁止逐字段重复** |
| 校验逻辑包装（`失焦或提交时若...`、`则拦截保存`、`系统不落库` 等）| 校验规则段（§4.4）或提示消息表（§4.5）按职责分工 |
| IMG-XX 引用（`以 IMG-XX 为准`、`见 IMG-XX` 等）| `.2 原型图` 段集中引用（参 §4.3 通用规则）|
| 推托语（`按业务定`、`以实现为准`、`按截图实际值`、`按业务配置` 等）| 必须写具体业务值或行业标准 |
| UI 反馈描述（`红字提示`、`拦截保存`、`不落库` 等）| 操作流程或校验规则 |
| 校验条件前缀（`仅在<X>时为必填`）| 「必填」列写 `条件必填` + 约束规则注明触发条件，不要混入校验描述 |

❌ 反面示例：`不能为空；同货主下唯一；防 SQL 注入/XSS；长度建议 1~50 字符`
✅ 正面示例：`不能为空；同货主下唯一；长度 1~50 字符`

##### 总结：判别流程

```
看到字段 → 看 UI 控件类型
├─ 输入类（文本/数值/金额/日期/上传 等用户键入）→ §3.1.4 输入类，按行业标准库推导
└─ 选择类（下拉/单选/复选/级联/树形 等用户选择）→ §3.1.4 选择类，按截图实际选项写
```

#### 3.1.5 默认值推导铁律（按业务惯例 + UI 控件类型）

**默认值列写什么（3 类·一律以原型为准，2026-06-28 用户重定义）**：

1. **有默认值** → 写默认值本身（单选/复选**默认选中项**如"无物流发货"、预填文本/数值、只读字段默认显示如"-"）。
2. **无默认值·有占位提示** → 写**占位提示文案，一字以原型为准**（如"请选择"、"请输入银行流水号/交易号"、"请选择日期"）。
3. **无默认值·无占位提示** → 写「空」或「无」。

> 默认值列 = "字段打开时默认显示什么"。占位提示本身不是默认值，但无默认值时该列照原型显示占位文案（内容自身可区分：占位文案 vs 真实默认值）。**禁用 `空（显示"X"）` 旧包装，直接写占位原文。**

**上述 3 类只适用「新增类（空表单）」**。**编辑 / 查看类不一样**——表单打开时已带出该条记录的已保存值，不存在占位：

4. **编辑类**（带出已保存值，可改）→ 默认值列写 **`回显`**（即"系统回填该记录已保存值"，如付款方式回显"转账"、金额回显"5000.00"）；该记录某字段原本为空 → 回显空。
5. **查看类**（只读详情）→ 默认值列写 **`回显`**（只读）；记录中为空的字段按原型只读占位显示（多为 `-`）。

> 一句话：**新增看占位/默认值，编辑/查看一律 `回显`**。判定"新增 vs 编辑 vs 查看"依据功能点类型（操作流程入口）。

**形态无关（PC / APP / 小程序 / H5 / 网站 一律通用）**：默认值规则只看"字段打开时默认显示什么，以原型为准"，与端无关。你**圈选 / 点选原型后，采集层已带回该区域真实文案**（zoneText / zoneHTML / 占位 / 默认选中项），AI **直接读采集到的原型真实文案**落默认值列，**禁臆造**。移动端 / 网页端控件按下表映射（仍以原型实际显示为准）：

| 移动端 / 网页控件 | 默认值写法 |
|---|---|
| 选择器 Picker / 底部弹层选择 | 占位原文（如 `请选择`）或默认选中项 |
| 级联 Picker（省市区 / 分类）| 占位原文（如 `请选择地区`）|
| 日期 / 时间 Picker | 占位原文（如 `请选择日期`）或默认值（如 `今天`）|
| 开关 Switch | 默认态（开 / 关，以原型为准）|
| 步进器 Stepper / 计数器 | 默认数值（如 `1` / `0`）|
| 单选 Radio / 标签选择 / 分段控件 | 默认选中项 或 占位 |
| 上传（图片 / 附件）| `无`（无占位）|
| 滑块 Slider / 评分 Rate | 默认值（以原型为准）|

**禁设"统一标准占位文案"（关键）**：占位文案**没有标准模板**，**一律取原型该字段实际显示的占位原文**（如 `请选择` / `请输入银行流水号/交易号` / `选填` / `必填，将同步通知申请方`，一字照搬）。上面控件类型表里的 `请选择` / `请输入<字段名>` 只是**无原型时的兜底举例，不是要统一套用的标准**。圈选 / 点选流程下采集层必带回真实占位，**有真实占位就一律用真实占位**——PC / APP / 小程序 / H5 / 网站全部一样。

**推导优先级**（高 → 低）：

1. 截图/原型明示默认值（红字标注、预选项、UI 标识）或占位提示 → **一律以原型为准**
2. 用户 prompt 明示 → 以用户指示为准
3. **业务惯例默认值库**（见下方）→ 仅在原型未明示时 AI 自动套用
4. **UI 控件类型默认**（见下方）→ 兜底

**UI 控件类型默认值**（无截图/无业务惯例时的兜底；**有原型一律以原型占位/默认选中项为准**）：

| 控件类型 | 默认值（无原型时兜底）|
|---|---|
| 文本输入框 / 文本域 | 占位原文（以原型为准），如 `请输入<字段名>` |
| 数值 / 金额输入框 | 占位原文 或 `0`（按业务）|
| 日期选择器 | 占位原文（如 `请选择日期`）或 `今天`（按业务）|
| 日期范围选择器 | 占位原文 或 `近 30 天`（按业务）|
| 单选 / 单选按钮组 / 选项卡 | **截图默认选中项**（如 `无物流发货`）或 业务最常用态 |
| 复选框组 | 截图勾选项 或 `未勾选` |
| 下拉选择（单选）| 占位原文（以原型为准），如 `请选择` |
| 下拉选择（多选）| 占位原文 或 `全部` |
| 级联 / 树形选择 | 占位原文（以原型为准），如 `请选择` |
| 只读字段（随主数据带出）| 默认显示值，如 `-` |
| 上传 | `无`（无占位）|
| 开关 | 业务常用态（关闭居多）|

**业务惯例默认值库**（按字段语义自动套用）：

| 字段语义 | 业务惯例默认值 |
|---|---|
| **状态机字段** | |
| 业务对象初始状态 | 业务定义的初始态（如商品 `草稿`、订单 `未支付`、单据 `待审核`）|
| 启用/停用类 | `启用` 或 `开启` |
| **商品类** | |
| 商品类型 | `实物商品`（电商默认）|
| 销售渠道 | `线上销售` 或 业务最常用 |
| 销售模式 | `现货`（vs 预售）|
| 保质期管理 | `关闭`（非生鲜默认）|
| 物流模式 | `仓配`（最常见）|
| 商品状态（新建态）| `草稿` |
| **财务 / 订单类** | |
| 币种 | 按部署地区（CN→`CNY`、BR→`BRL`、US→`USD`）|
| 金额 / 价格 | `空` 或 `0.00` |
| 税率 | `0` 或 业务默认税率 |
| 订单状态（新建）| `未支付` 或 `待支付` |
| **查询 / 筛选类**（详见 §3.5） | |
| 全部 X 下拉 | `全部` |
| 状态 Tab 默认选项 | `全部` 或业务最常关注的态 |
| 日期范围 | `近 30 天` 或业务定义 |
| **时间 / 地区 / 语言** | |
| 时区 | 按部署地区（CN→`Asia/Shanghai`、BR→`America/Sao_Paulo`）|
| 语言 | 按部署地区（CN→`zh-CN`、US→`en-US`）|
| **日期时间显示格式（UI 展示层，含一切给用户看的时间）** | 按部署地区——**默认 CN→`YYYY-MM-DD HH:mm:ss`**；BR→`DD/MM/YYYY HH:mm:ss`；US→`MM/DD/YYYY hh:mm:ss AM/PM`。**覆盖所有用户可见的时间展示**：列表时间列、详情、消息内容、**操作日志查看界面的"操作时间"**、单据时间等，一律按部署地区本地化。**AI 写 PRD 前必须主动询问用户部署地区**（"本次 PRD 是面向中国还是巴西/其他地区？"），不得默认按 CN 输出。地区线索（如 `巴西` / `Brasil` / `BRL` / `NCM` / `CEST` / `CFOP` / `葡语` 等）出现时直接判定为 BR；未询问即按默认输出 = 违规 |
| **日期时间存储格式（数据库 / API 落库层）** | 固定 `YYYY-MM-DD HH:mm:ss`（ISO 8601 国际标准，跨地区一致；不随 UI locale 变化）。**注意：操作日志等底层"存储值"为 ISO，但"展示"给用户时仍按上一行部署地区格式本地化**——即"底层存 ISO、展示随地区"，二者不冲突。**对外 PRD / 原型标注的时间字段，只呈现"地区展示格式"（如 BR `DD/MM/YYYY HH:mm:ss`），不写出"底层存储 ISO"说明（属内部实现，不对外暴露）** |
| **POS / 收银类** | |
| 班次状态 | `未开班` |
| 收银员 / 操作员 | `登录用户`（自动带入）|
| 支付方式（多选）| `现金 / 扫码 / 刷卡` 全启用 |
| **WMS / 仓储类** | |
| 入库类型 | `采购入库`（最常见）|
| 出库类型 | `销售出库`（最常见）|
| 当前仓库 | `用户所属仓库`（自动带入）|
| **商城前台类** | |
| 收货地址 | `用户默认收货地址`（自动带入）|
| 支付方式 | `用户上次使用的支付方式` 或 业务推荐 |

**默认值列禁写内容**：

| 禁写 | 应该写 |
|---|---|
| `按业务定` / `按业务配置` / `按截图实际值` | 有默认值写默认值；无默认值写占位原文（以原型为准）或 `空`/`无` |
| `以 IMG-XX 为准` / `按实现` / `以实现为准` | 具体默认值，或占位原文 |
| `空（显示"X"）` 旧包装写法 | 无默认值直接写占位原文（如 `请选择`、`请输入<字段名>`，§3.1.5）|

❌ 反面示例：`| 商品类型 | 选项卡按钮组 | 是 | 按截图实际值 | ... |`
✅ 正面示例：`| 商品类型 | 选项卡按钮组 | 是 | 实物商品 | ... |`

#### 3.1.3 类型列禁止用旧术语

| 禁用术语 | 替换为 |
|---|---|
| `文本` | `文本输入框` |
| `下拉` | `下拉选择` |
| `单选组` | `单选按钮组` |
| `选项卡/按钮组` | `选项卡按钮组` |
| `数值` | `数值输入框` |
| `数值 + 单位 KG` | `数值输入框（单位 KG）` |
| `图片上传` | `上传（图片）` |
| `多行文本` | `文本输入框` 或 `文本域` |

### 3.1.6 字段规范分块标题禁臆造

字段规范表的分块标题（如 `**1、基本信息**` / `**2、价格库存**` / `**3、税务信息**` 等加粗序号小节）**必须有截图依据**——弹窗 Tab 标签 / 页面分组标题 / 折叠面板标题 / fieldset 边框标题 等可见 UI 元素。

**判定**：

| 截图特征 | 字段规范输出 |
|---|---|
| 弹窗顶部明确显示 N 个 Tab（如"基础信息 / 规格信息 / 价格库存"）| 字段规范按 N 块分段，**分块名一字不易取自截图 Tab 真实文字**，**不得改名为同义词**（如"基础信息" ≠ "基本信息"） |
| 页面显示折叠面板 / fieldset 边框 / 视觉分组（含分组标题）| 按可见分组数分段，分块名取截图真实文字 |
| 截图无明显分组特征（单片表单，所有字段平铺）| **字段规范直接平铺为单表**，**禁止**自创 `**1、基本信息**` `**2、xxx**` 等分块标题 |
| 截图描述含"X 个 Tab"但未列 Tab 名 | 进「待确认项」让用户补 Tab 真实名称，**禁止**先按推测分块 |

**禁止行为**：

- ❌ 截图无"税务信息"分组却在字段规范加 `**4、税务信息**` 段
- ❌ 截图 Tab 名为"基础信息"却改写为"基本信息"
- ❌ 截图未显示任何分组特征却按业务逻辑（基础 / 价格 / 规格 / 税务）自创分块
- ❌ 用 `**X、xxx**` 加粗序号分块但 X 数量与截图 Tab 数量不符

**理由**：分块标题是 UI 元素的一部分——开发按 PRD 实现时会照 PRD 创建对应的 Tab/折叠面板/分组。若 PRD 凭空生成分组，开发会按 PRD 做出截图里没有的 UI，产生 UI 与 PRD 不一致；同时分块名改写为同义词会让 QA 测试时找不到对应控件。

### 3.2 字段范围铁律（仅收录数据字段）

字段规范表内仅收录**触发数据写入或回显的输入控件**：文本、下拉、单选、复选、上传、日期、数值、级联、开关、文本域、富文本等。

**禁止**收录任何仅触发"动作执行"或"页面跳转"的元素：
- 操作按钮（保存/取消/确定/重置/关闭/复制/导出/导入/打印/启用/停用/删除/新增/编辑 等）
- 操作链接（行内删除/编辑/查看/详情列、批量删除/批量启用入口）
- 底部操作区、弹窗底栏、顶栏操作区、工具栏 整段
- 关闭图标 ✕、面包屑、返回箭头等纯导航元素

按钮与操作交互一律由 `用例规则-操作流程` 承载。如需描述按钮位置/可见性/权限，写在 `位置` 段或 `操作流程` 步骤中。`查询` 功能 `1、查询条件` 表内的「查询」「重置」按钮亦禁止。

### 3.3 取字段源（按功能点类型）

- `查询`：固定拆为 `1、查询条件` + `2、列表字段`。
- `查看`：默认值统一写 `回显`；明确「不可编辑、不可触发写入校验」。详细判定见 §3.3.1。
- `新增 / 编辑 / 设置` 及同类：取「表单字段」。
- 严禁把列表字段、查询条件混到表单字段中输出。

#### 3.3.1 「查看」功能场景判定铁律

**判定标准：以截图视觉特征为唯一依据**——不以"字段是否很多 / 业务是否复杂"等业务判断替代视觉判定。

**两种场景：**

| 截图视觉特征 | 场景 | 写法 |
|---|---|---|
| 列表中 **单据号 / 数据标识 显示为蓝色可点击链接** ✅<br>**或** 操作列有显式的 **「查看」按钮** ✅<br>（两者出现任一即满足） | **详情页查看** | **必须单列** `<编号> 查看` 一节；字段口径 = 进入详情页后的只读字段，分区块（基本信息 / 价格规格 / 税务信息 等）描述 |
| 单据号**不是**蓝色链接（普通灰色文本 / 纯展示）❌<br>**且** 操作列**没有**「查看」按钮 ❌ | **列表页直接查看** | **默认不单列**「查看」功能点；查看口径并入 `查询` 节描述（在`操作流程`末尾加一句"用户可在列表页直接查看数据，无独立详情页"即可）；字段口径 = `查询` 节的 `2、列表字段`，**禁止**重复列字段表 |

**例外（仅以下四种情况，"列表页直接查看"场景仍须单列或重命名）：**

1. **权限差异**：「查询」与「查看」分配给不同角色 / 不同字段可见性需要单列承载权限规则。
2. **审计 / 留痕强制**：业务明确要求「查看」动作必须打操作日志（即便铁律 §4.7 默认查询/查看不打日志，业务强制则单列并破例打日志，同时在 PRD 注明破例原因）。
3. **跨菜单跳转**：列表行点击会跳转到另一个独立页面（即使视觉上没有蓝色链接样式），本质是详情页查看的视觉变体——按"详情页查看"处理并单列。
4. **本菜单无独立「查询」节**：该菜单的功能点中没有「查询」、原"查看"节实质承担列表展示+筛选+只读查看的全部职责——此时**把"查看"重命名为"查询"**（不再单列"查看"）；操作流程末尾仍写"用户可在列表页直接查看数据，无独立详情页"。

**未提供截图时**：先向用户问最少必要问题确认场景，**禁止猜测**、禁止默认"两端都写"或"两端都不写"。

**禁止写法（无论哪种场景）：**
- 空壳节：`<编号> 查看` 节只写"以查询为准""见上文""字段同列表"等无效内容。
- 字段重复：列表页直接查看场景下，把 `查询` 的列表字段在 `查看` 节重新抄一遍。
- 业务判断替代视觉判定：仅因"字段太多"或"业务复杂"就单列查看节，但截图不支持。

#### 3.3.1.1 详情页查看「操作流程」标准句式（三步全部固定文案）

**详情页查看**场景下，`用例规则 - 操作流程` 三步全部为固定文案——除占位符外不得改写、不得替换同义词。

| 步号 | 固定文案 |
|---|---|
| 1 | `用户在<模块>列表，点击操作列的「查看」或<数据标识>，系统进入查看界面。` |
| 2 | `用户浏览只读详情信息。` |
| 3 | `用户点击「取消」或关闭，系统将不进行任何操作并返回列表或关闭浮层。` |

**禁止替换的同义词与变体**：

- 第 1 步 `操作列` 不得改为 `行内` / `操作栏` / `末列` / `目标行`。
- 第 1 步 `系统进入查看界面` 不得改为 `打开` / `跳转` / `弹出` / `进入详情` / `打开浮层`。
- 第 2 步 `用户浏览只读详情信息` 不得改为 `用户阅览` / `用户查看数据` 等同义改写。
- 不得新增 / 合并 / 省略任一步骤；不得插入 `等价入口` / `若页面提供` 等模糊修饰。

**占位符填写铁律（必须从用户当次截图识别，禁止套用默认值）：**

- `<模块>`：当前菜单名称（如 `商品库列表`、`门店商品列表`、`出库单列表`、`仓库列表`、`部门列表`、`人员列表` 等），取自菜单结构、与该功能点位置一致。
- `<数据标识>`：**列表中蓝色可点击链接对应的字段名**——AI 须从用户当次提供的截图中识别，**不得**猜测、**不得**套用默认值或上一份 PRD 取过的值。常见取值（仅作参考，实际以截图为准）：
  - **商品类**：`商品编号` / `商品ID`
  - **单据类**：`单据号` / `出库单号` / `入库单号` / `订单号` / `退货单号` / `采购单号`
  - **主数据类**：`仓库编号` / `部门编号` / `人员编号` / `客户编号` / `供应商编号`
  - **其它业务标识**：以截图实际显示的字段名为准

**多蓝色链接处理铁律（先二次确认再输出）**：

若截图中**同一行出现多个蓝色可点击字段**（如同时存在"商品编号"与"商品名称"均为蓝色），AI **必须停下来先向用户二次确认**，不得直接输出。

**二次确认标准话术（AI 输出）**：

> 截图中我识别到 `<功能点位置>` 列表的同一行有多个蓝色可点击字段：`<字段1>`、`<字段2>`（…）。请确认查看入口的写法：
>
> 1. 全部作为查看入口（写为 `或<字段1>或<字段2>…`）
> 2. 仅取 `<字段X>` 作为查看入口
> 3. 其它（请补充说明）

**确认后行为**：

- 用户回答 1 → 第 1 步写：`用户在<模块>列表，点击操作列的「查看」或<字段1>或<字段2>…，系统进入查看界面。`
- 用户回答 2 → 第 1 步写：`用户在<模块>列表，点击操作列的「查看」或<字段X>，系统进入查看界面。`
- 用户回答 3 → 按用户具体说明执行。

**禁止行为**：

- **禁止**未问就自行列全部蓝色字段（即使全部如实列出也违规）。
- **禁止**未问就自动取"业务主键"或"展示名称"。
- **禁止**截图中没显示为蓝色的字段补进 `<数据标识>`——即使业务上常用。
- **禁止**为图方便跳过二次确认；只要识别到 ≥2 个蓝色字段就必须先问。

**截图模糊 / 无法确认蓝色字段数量时**：同样先向用户提最少必要问题确认，禁止臆测。

**特殊情形**：

- 若该菜单视觉特征**仅有操作列「查看」按钮、无任何蓝色链接**，则第 1 步简化为 `用户在<模块>列表，点击操作列的「查看」，系统进入查看界面。`（省略 `或<数据标识>` 段，其余两步不变）。
- **截图缺失时**：先向用户提最少必要问题确认蓝色链接对应的字段名，禁止臆测；不得默认套"商品编号"或上一份 PRD 的取值。

### 3.4 新增 / 编辑 默认值与唯一性

- `新增` 默认值固定：文本框（需输入）= `请输入<字段名>`；下拉/选择框 = `请选择`；复选框 = 第一项（不可用则取第一个可用项；均不可用则不选）。
- `请输入<字段名>` 的 `<字段名>` 必须与字段规范字段名一字不差；同页同类输入占位口径统一（如统一「请输入」，禁止与「请填写」混用）。
- 新增默认值优先级：业务配置默认值 > 系统内置默认值 > 空值。
- 联动显示字段需补充联动默认值规则。
- `编辑` 默认值统一为 `系统回填已保存值`；与 `新增` 口径**分离**，不可混写。
- `编辑` 字段规范与 `新增` 同结构、同校验维度；若有唯一性校验，规则中注明「编辑时排除当前记录自身」。
- 保存前归一化：文本去首尾空格；空串按规则转空值；数值仅保留合法格式后再校验。
- 保存失败时：保留用户已输入内容，不清空页面，定位到首个校验失败字段。

### 3.5 查询条件默认值

文本类默认空；下拉类默认「全部」；时间类按业务定义默认范围（如近 30 天）；除业务明确要求外，查询条件默认非必填。

---

## 4 用例规则（七项铁律）

### 4.1 七项标题（锁定文案与顺序）

每个功能点的 `用例规则` 须按下列顺序完整展开七项，标题文案与截图锁定：

`• 前置条件：` → `• 操作流程：` → `• 后置条件：` → `• 校验规则：` → `• 提示消息：` → `• 消息通知：` → `• 操作日志：`

- 七项标题**单独成行、行首顶格**，纵列 `•` 左缘对齐（"同一水平线"）。
- 各标题末尾必须有全角 `：`。
- 已废止 `• 留痕要求：`：禁止在 PRD 正文出现；审计/留痕由 `• 操作日志：` 承载。
- 若该小节已写单行 `请参考：…` 省略（见 §8），则**不得**再展开七项。

### 4.2 七项排版

- 短句可与标题同行；换行时正文次行起两空格缩进。
- `• 操作流程：` 独占一行，步骤 `  1、` / `  2、` … **全角顿号** + 两空格缩进（与前置/后置/校验规则保持一致）。
- `• 校验规则：` 下若分条，使用 `1、` / `2、` / `3、` 全角顿号 + 两空格缩进（详见 §4.4.2）。
- `• 前置条件：` 单条同行；多条用 `1、` / `2、` / `3、` 全角顿号 + 两空格缩进分条（详见 §4.2.1）。
- `• 提示消息：`、`• 操作日志：` 所附 Markdown 表格顶格。

#### 4.2.1 前置条件 规范

**结构三要素**（按需写，业务前置必有）：

| 要素 | 内容 |
|---|---|
| ① 登录 | `用户已登录<系统名称>` |
| ② 权限 | `具备「<完整菜单路径>-<功能点名>」权限` |
| ③ 业务前置 | 按功能点类型必写（见下表） |
| ④ 依赖前置 | 仅当确有系统外依赖时写（如"统一账号中心已配置该角色"）；通常省略 |

**业务前置标准句式（按功能点类型）**：

| 功能点类型 | 业务前置 |
|---|---|
| 新增（无依赖） | 无需业务前置 |
| 新增（有依赖主数据）| `<依赖主数据名>已存在可被引用`（如"商品分类已存在"）|
| 编辑 | `<模块>列表中存在目标<业务对象>` |
| 查看 | `<模块>列表中存在至少一条<业务对象>` |
| 删除 | `<模块>列表中存在状态为<可删除状态>的<业务对象>`（如"停用且无引用"）|
| 启用 | `<模块>列表中存在状态为"停用"的<业务对象>` |
| 停用 | `<模块>列表中存在状态为"启用"的<业务对象>` |
| 上架 | `<模块>列表中存在状态为"未上架/停用"的<业务对象>` |
| 下架 | `<模块>列表中存在状态为"已上架"的<业务对象>` |
| 改价格 | `<模块>列表中存在目标<业务对象>` |
| 发布到门店 | `<模块>列表中存在状态为"可发布"的<业务对象>`；可发布的目标门店清单不为空 |
| 导入 | 通常无；或 `导入模板已下载` |
| 导出 | 通常无；或 `当前查询结果集存在数据` |
| 打印 | `<模块>列表中存在目标<业务对象>`；`已有<X>模板数据可用` |
| 复制 | `<模块>列表中存在目标<业务对象>` |

**排版铁律**：

- **1 条**：同行写。
  ```
  • 前置条件：用户已登录零售连锁系统，具备「商品-商品管理-商品库-新增-无规格」权限。
  ```

- **2 条及以上**：**必须**按 `1、` / `2、` / `3、` 全角顿号 + 两空格缩进**分条**；**禁止**用 `；` 拼接成一段长文本。
  ```
  • 前置条件：
    1、用户已登录零售连锁系统，具备「商品-商品管理-商品库-删除」权限。
    2、商品库列表中存在状态为"停用"且无引用的商品。
  ```
  ```
  • 前置条件：
    1、用户已登录零售连锁系统，具备「商品-商品管理-商品库-发布到门店」权限。
    2、商品库列表中存在状态为"可发布"的商品。
    3、可发布的目标门店清单不为空。
  ```

注：「用户已登录<系统>，具备「...」权限」是一条复合句，作为 1 条不拆分；业务前置 / 依赖前置作为独立的条目。

**禁止**：
- ❌ 业务前置遗漏（编辑/删除/启用/停用/上架/下架/改价/发布/打印/复制 等必须含业务前置）
- ❌ 多条混在一段长文本里（如 4 条用 `；` 拼接成一句）
- ❌ 用模糊措辞（如"具备权限"无菜单路径、"数据存在"无具体状态）
- ❌ 与 §4.4.2 校验规则的编号格式不一致（必须都用 `1、` 全角顿号）

#### 4.2.2 后置条件 规范

**核心要求**：按功能点类型采用统一标准句式；每条句子必含「结果状态 + 可见位置」；句末统一全角 `。`。

**15 类标准句式**：

| 功能点类型 | 标准句式 |
|---|---|
| 新增 | `生成一条<对象>数据<，状态为"<默认状态>"，可选>；可在<模块>列表查询到。` |
| 新增子分类 / 子节点类 | `在父级<对象>下生成子<对象>节点；可在<模块>列表查询到。` |
| 编辑 | `<对象>数据更新为最新编辑结果；<模块>列表数据同步刷新。` |
| 删除 | `<对象>从<模块>列表移除（详见术语：软删除）。` |
| 启用 | `所选<对象>状态变为"启用"；<模块>列表状态列同步刷新。` |
| 停用 | `所选<对象>状态变为"停用"；<模块>列表状态列同步刷新。` |
| 上架 | `所选商品进入"销售中"状态；<模块>列表状态列同步刷新。` |
| 下架 | `所选商品进入"已下架"状态；<模块>列表状态列同步刷新。` |
| 改价格 | `所选商品价格更新为提交值；<模块>列表价格列同步刷新。` |
| 发布到门店 | `所选商品在目标门店"门店商品"列表可见。` |
| 导入 | `合法行写入<模块>；非法行不写入，错误明细可在"下载中心"获取。` |
| 导出 | `导出文件已生成；可在"下载中心"下载（保留 7 天）。` |
| 打印 | `生成打印任务并完成打印机输出；打印记录可在"打印任务"查询。` |
| 复制 | `生成一条新的<对象>副本（状态默认为"停用"，需手工启用）。` |
| 查询 | `列表展示与当前筛选条件一致；可继续执行<可选后续动作>等操作。` |
| 查看 | `不产生数据变更；可继续浏览或返回<模块>列表。` |

**禁止写法**：

- ❌ 同类功能用不同措辞（如编辑类有的写"X 信息更新"，有的写"X 数据更新为最新编辑结果"——必须统一）
- ❌ 缺"可见位置"（如只写"X 信息更新"不写"列表同步刷新"）
- ❌ 用模糊表达：`进入可售相关状态` / `按规则同步` / `以实现为准` / `（按业务定）` 等
- ❌ 删除类**重复抄写**「软删除策略」全文——应引用 §3 术语「软删除」
- ❌ 导出/打印套话化（仅写"完成打印输出"无业务信息）

**软删除策略全局术语**：

PRD 必须在 §3 术语 / 图例定义 章节定义：

```
| 软删除 | 删除采用逻辑删除：被删除数据保留底层记录，可由具备「数据恢复」权限的角色按统一恢复入口还原；
        如业务确需硬删除须后台二级审批后执行 |
```

后续删除类功能点的后置条件**只**写：`<对象>从<模块>列表移除（详见术语：软删除）。`

**多条后置条件**：

- **1 条**：同行写。
- **2 条及以上**：**必须**按 `1、` / `2、` / `3、` 全角顿号 + 两空格缩进**分条**；**禁止**用 `；` 拼接成一段长文本。

示例：
```
• 后置条件：
  1、所选商品价格更新为提交值。
  2、商品库列表价格列同步刷新。
  3、变更同步到门店商品视图。
```

### 4.3 操作流程

- 操作流程涉及列表类页面时，列表名称须冠以**具体系统模块名称**，禁止裸写「列表」「在列表」。
  示例：`用户在仓库管理列表，选择目标行，点击「启用」`。
- 凡存在「取消」按钮（弹窗或新页面），操作流程必须包含：`用户点击「取消」，系统将不进行任何操作`。
- 列表内联编辑（编辑态逐行修改）统一句式：`用户可在编辑态下，对<业务名称>列表中各行的"<字段1>""<字段2>"进行修改。`——禁止「用户可对每一行修改」「支持行内编辑」等模糊表达。
- **填字段步骤禁列具体字段名**：操作流程描述"填表/选项"步骤时，**禁止枚举具体字段名**（字段已在字段规范表逐行列出，操作流程重复=双重维护）。统一句式：
  - 通用单表单：`用户填写表单字段，点击「保存」按钮。`
  - 多 Tab 弹窗/页面：`用户在"<Tab1>"、"<Tab2>"等 Tab 间切换填写表单字段，点击「保存」按钮。`
  - 含字段联动：`用户填写表单字段（选择"<X>"后，"<Y>"字段自动加载），点击「保存」按钮。`
  - **字段 ≤ 2 个的简单场景可列字段名**（如改价仅 1 个字段 → `用户输入新价格`）
  - ❌ `用户依次填写商品名称、商品分类、规格、计量单位、品牌等字段，点击「保存」`
  - ✅ `用户填写表单字段，点击「保存」按钮`
- **字段规范 / 用例规则全 7 项禁止内嵌 IMG-XX 引用**：图片标识**仅在 `.2 原型图` 段集中引用**；字段规范表、字段规范分块标题、字段规范节标题、前置条件 / 操作流程 / 后置条件 / 校验规则 / 提示消息 / 消息通知 / 操作日志均禁止任何形式的图片编号嵌入：`（IMG-XX）` / `（见 IMG-XX）` / `详见 IMG-XX` / `在 IMG-XX 中` / `在 IMG-XX 选择` / `通过 IMG-XX` / `含 IMG-XX` / `以 IMG-XX 为准` / `仅 IMG-XX` / `IMG-XX 处` / `IMG-XX 同类能力` 等。
  ❌ `**1、基本信息（IMG-03）**` ✅ `**1、基本信息**`
  ❌ `\| 保质期管理 \| 单选 \| 是 \| 以 IMG-06 选中项为准 \| ...` ✅ `\| 保质期管理 \| 单选 \| 是 \| 开启 \| ...`
  ❌ `用户点击「选择门店」，在 IMG-24 中切换...` ✅ `用户点击「选择门店」，在门店选择弹窗中切换...`

#### 4.3.1 入口动作标准句式（按 UI 类型区分页面 vs 弹窗）

操作流程中「点击按钮 → 跳转新页面 / 打开弹窗」类入口动作，**按截图实际 UI 类型采用以下二选一固定句式**：

| UI 类型 | 标准句式 |
|---|---|
| **跳转独立页面**（全屏表单 / 全屏设计器） | `用户<在 X 列表，>点击<操作列的>「<按钮名>」按钮，系统进入到<新增/编辑/...>页面<，回显数据>。` |
| **打开弹窗**（modal / 抽屉 / 弹层） | `用户<在 X 列表，>点击<操作列的>「<按钮名>」按钮，系统打开<新增/编辑/...>弹窗<，回显数据>。` |

**填写规则：**

- 「按钮」**必加**：`点击「<按钮名>」` 必须带后缀 `按钮`（链接组件则用 `链接`，按截图判断）。
- 多个并列按钮：`「<按钮1>」或「<按钮2>」按钮`（按钮在末尾统一一次，不重复标）。
- 编辑类入口（点击操作列「编辑」）：列前缀必写 `操作列的`（不写 `目标行` / `行内`）。
- 编辑类必含回显：`系统打开/进入到编辑页面/弹窗，回显数据`（不写"并回显""并回显数据"——拆为独立短句）。
- **页面 vs 弹窗判定**：以**截图视觉为准**——跳转新 URL / 全屏覆盖 → 页面；浮在当前页之上 / 有遮罩 → 弹窗。截图不确定时**先向用户二次确认**。

**禁止表达：**

- ❌ `系统打开新增/编辑表单`（"表单"是控件不是页面/弹窗类型，含糊）
- ❌ `系统打开 / 跳转 / 弹出`（单个动词无对象，要写"打开 X 弹窗"或"进入到 X 页面"）
- ❌ `点击目标行的「编辑」` → 改为 `点击操作列的「编辑」按钮`
- ❌ 省略 `按钮` / `链接` 后缀
- **UI 不确定性括注铁律**：操作流程与字段规范中**禁止**使用 UI 不确定性括注，包括但不限于 `（若页面提供）` / `（若存在中间弹层）` / `（若提供）` / `（若需要）` / `（若存在）` / `（若有）`（仅当用于"业务条件存在与否"时除外，如"一品多码（若有）"指功能开关下条件出现）。所有 UI 元素的存在性**必须从截图确认**：截图显示存在则直接写、不加括注；截图不显示则不写；截图不确定则**先向用户二次确认**（参 §3.3.1.1 多蓝色处理思路），禁止用"若..."占位。
- **推托性括注与措辞铁律**：操作流程、字段规范、用例规则、校验规则等正文**禁止**使用"延迟到实现/截图/产品设计"的推托表达，包括但不限于：
  - 括注类：`（以实现为准）` / `（以图为准）` / `（以 IMG-xx 为准）` / `（以模板支持为准）` / `（以状态枚举为准）` / `（以枚举配置为准）` / `（以产品状态规则为准）` / `（多 Tab 或单页滚动，以 IMG-xx 为准）` / `（按需）` / `（可选）` 等。
  - 嵌入类：`；布局以 IMG-xx 为准` / `；结构以 IMG-xx 为准` / `按产品设计` / `按业务约定` / `按业务实际` 等。
  - 选项延伸类：`或等价入口` / `或等价提交入口` / `或等价状态` / `或等价 xxx` 等。
- 替代方式：
  1. 若可由截图确认 → **直接照实写**（不加"以 IMG-xx 为准"，因为原型图已在 `.2 原型图` 段引用对应 IMG 编号）；
  2. 若由业务/枚举配置决定 → **直接列出当前业务枚举值**（如"单位类型"枚举为"计数 / 计重 / 计长 / 计积"，不写"以枚举配置为准"）；
  3. 若信息缺失或与用户确认 → **先问最少必要问题再写**，禁止用括注占位。

#### 4.3.2 二次确认 / 配置类弹窗的「确认 + 取消」双路径必写

操作流程涉及以下两类弹窗时，**必须**分别写出「主操作」和「取消」两条路径作为流程结束节点：

| 弹窗类型 | 典型触发动作 |
|---|---|
| 二次确认弹窗 | 删除 / 启用 / 停用 / 上架 / 下架 / 改价 / 发布 / 复制 等不可逆操作 |
| 配置类弹窗 | 新增 / 编辑 / 发布到门店 / 导入 / 导出 / 批量编辑 等含「取消」按钮的弹窗 |

**正向路径必含 2 要素**：① 成功反馈文案（如 `"删除成功"` / `"导入完成：成功 X 条，失败 Y 条"`）② UI 落点（关闭弹窗 / 列表自动刷新 / 状态字段更新 / 返回列表 等）。

**取消路径固定句式（按入口 UI 类型分流）**：

| 入口 UI 类型 | 取消固定句式 |
|---|---|
| **弹窗类**（modal / 浮层 / 抽屉，含二次确认/配置类弹窗）| `用户点击「取消」，系统关闭弹窗，不执行任何操作。` |
| **页面类**（独立全屏页面，如商品库新增、价签设计器等）| `用户点击「取消」，系统返回<X>列表，不执行任何操作。` |
| **纯查看弹窗**（仅"关闭"按钮、无"取消"按钮）| 按 §3.3.1.1 三步固定文案 |

**禁止口语化变体**：「取消则不变化」「关闭弹窗即可」「将不进行任何操作」「不进行操作并返回」 等。

**示例（以删除为例）**：

✅ 正确：
```
1、用户在商品库列表，点击操作列「删除」按钮，系统弹出二次确认弹窗"确认删除该商品？删除后不可恢复"。
2、用户点击「确认」，系统执行删除并提示"删除成功"，列表自动刷新。
3、用户点击「取消」，系统关闭弹窗，不执行任何操作。
```

❌ 错误（缺取消分支）：
```
1、用户在商品库列表，点击操作列「删除」按钮，系统弹出二次确认弹窗...
2、用户点击「确认」，系统执行删除并提示"删除成功"。
```

**不适用情况**：
- 跳转独立页面（非弹窗）→ 按 §4.3 通用「取消」规则写一句即可，不强制双路径结束节点
- 纯查看弹窗（仅「关闭」按钮，无「取消」按钮）→ 不适用，按详情页查看规则 §3.3.1.1 处理

### 4.4 校验规则

#### 4.4.1 职责边界铁律（与 §4.5 提示消息表分工）

**校验规则与提示消息表的职责严格分离**，避免重复：

| 校验类型 | 承载位置 | 不在这里 |
|---|---|---|
| **字段级校验**（必填 / 格式 / 长度 / 唯一性 / 范围 / 防注入 / 枚举不合法）| ✅ §4.5 提示消息表（列 2 + 列 3）| ❌ 校验规则**不再重复**写这些 |
| **跨字段校验**（如开始时间 ≤ 结束时间、价格 ≤ 原价、规格组合不重复）| ✅ 校验规则 | ❌ 字段表无法表达 |
| **状态校验**（如已下架商品不能再下架、已删除数据不能编辑）| ✅ 校验规则 | ❌ |
| **权限校验**（如角色无权操作、跨货主访问拦截）| ✅ 校验规则 | ❌ |
| **业务逻辑**（如库存不足、最低售价保护、引用拦截、状态流转）| ✅ 校验规则 | ❌ |
| **异常处理**（如接口超时、并发冲突、重复提交）| ✅ 校验规则 | ❌ |

**结论**：校验规则**仅承载 5 类非字段级校验**——跨字段 / 状态 / 权限 / 业务逻辑 / 异常。字段级校验全部由 §4.5 提示消息表承载，校验规则**禁止重复写**。

#### 4.4.2 句式与排版

- 统一句式：`当<触发条件>时，若<拦截条件>，则<处理动作>并提示"<文案>"，系统<失败后行为>。`
- **1 条**：同行写：`• 校验规则：当...时，若...，则...。`
- **2 条及以上**：**必须**按 `1、` / `2、` / `3、` 全角顿号 + 两空格缩进**分条**；**禁止**用 `。` 拼接成一段长文本，与前置/后置条件分条规则一致。

```
• 校验规则：
  1、当...时，若...，则...。
  2、当...时，若...，则...。
  3、当...时，若...，则...。
```

- 标题独占一行：`• 校验规则：` 单独一行；其下分条次行起**两空格缩进** + `N、` + 校验内容。
- 每条独立一句；**禁止**多种校验类型混杂在一句里。

#### 4.4.3 必含要素（每条规则）

| 要素 | 写法 |
|---|---|
| 触发条件 | 何时触发（如"点击保存"、"点击下架"、"接口返回 5xx"）|
| 拦截条件 | 什么状态/数据下要拦截（如"所选数据状态为'已下架'"、"库存为 0"）|
| 处理动作 | 拦截 / 跳转 / 重试 / 回滚 等 |
| 提示文案 | 用 `"..."` 引号包裹的具体业务文案 |
| 失败后系统行为 | 如"系统不落库"、"系统不变更状态"、"系统保留用户输入"|

#### 4.4.4 单条校验边界（防"多校验塞一条"）

**1 条 = 1 个完整"触发→拦截→动作→提示→失败后行为"链**。出现以下任一信号**必须拆分**：

- 字数 ≥ 80
- ≥ 2 个 `当...时` 或 `若...`
- 含 `；` 分隔的并列校验
- 含枚举式罗列（"包括... ；... ；..."）

**非校验内容必须移出校验规则**：

| 内容 | 迁移至 |
|---|---|
| 错误反馈（下载按钮 / 进度条 / 错误弹窗）| 操作流程 |
| 业务策略（"默认允许 X 策略" / "异步处理"）| 操作流程或后置条件 |
| 字段映射（"列名匹配模板"）| 操作流程 |
| UI 反馈（按钮可见性 / 控件置灰）| 操作流程或字段规范 |

❌ `模板版本不匹配...；字段映射...；行级校验...；下载错误明细...；默认策略...`（5 件事塞 1 条）
✅ 拆 3 条独立校验 + 错误反馈进操作流程 + 业务策略进后置条件

#### 4.4.5 写法对照（正确 vs 错误）

**✅ 正确写法（仅写非字段级校验，按 `1、2、3、` 分条）**：

```
• 校验规则：
  1、当用户点击保存时，若商品规格行集合为空，则拦截提交并提示"请至少维护一条规格"，系统不落库。
  2、当用户点击保存时，若所选商品分类已被停用，则拦截提交并提示"所选分类已停用，请重新选择"，系统不落库。
  3、当用户点击保存时，若当前角色无"商品-新增"权限，则拦截并提示"无权限执行此操作"，系统不展示新增入口。
  4、当系统检测到 5 秒内同一用户重复提交时，则拦截后续提交并提示"请勿重复提交"，仅保留首次成功记录。
```

**❌ 错误写法（混入字段级校验，与提示消息表重复）**：

```
• 校验规则：
  当用户对任一必填字段失焦时，若未输入或未选择，则在该字段下方红色提示"请输入/请选择"，系统不允许进入下一步。
  当用户对格式类字段失焦时，若格式不合法，则在该字段下方红色提示具体规则说明，系统不落库。
  当用户点击提交时，若商品编号在同货主下已存在，则拦截提交并提示"商品编号已存在，请重新输入"，系统不落库。
```
（以上字段级校验**已在 §4.5 提示消息表覆盖**，校验规则不应重复）

#### 4.4.5 无非字段级校验时

若本功能点**无任何**跨字段、状态、权限、业务逻辑或异常类校验，写：

```
• 校验规则：字段级校验见提示消息表；本功能点无跨字段、状态、权限、业务逻辑或异常类校验。
```

不得为凑数而把字段级校验抄进来。

### 4.5 提示消息

- 有内容时，在 `• 提示消息：` 标题下、表格前先写一行（次行起两空格缩进，不加项目符号）：`在当前页面的字段下面红色字体提示：`。
- **字段表列固定（3 列）**：`字段名称` `未填写/未选择提示` `输入错误提示`。
- 表头模板：`| 字段名称 | 未填写/未选择提示 | 输入错误提示 |`
- 分隔行：`| --- | --- | --- |`
- 无内容时统一写 `无。`。

#### 4.5.1 列填写语义（关键铁律）

| 列 | 触发场景 | 适用字段 |
|---|---|---|
| **未填写/未选择提示** | 用户**未填写文本** / **未选择下拉、单选、复选、级联** 时显示 | **仅** `必填=是` / `必填=条件必填` 字段；`必填=否` 字段此列填 `-` |
| **输入错误提示** | 用户**已输入但不符合约束规则** 时显示（含格式错误、长度超限、唯一性冲突、范围越界、枚举不合法、防注入命中 等）| **所有有输入约束的字段**（必填+非必填均需写，因为非必填字段一旦用户输入也需校验）|

#### 4.5.2 触发场景拆解（按字段必填属性）

| 字段必填值 | 未填写/未选择提示 | 输入错误提示 |
|---|---|---|
| **必填=是** | 必填——用户没填/没选时提示 | 必填——用户填了但不符合约束时提示 |
| **必填=否** | `-`（非必填无此触发场景）| 必填——用户填了但不符合约束时提示 |
| **必填=条件必填** | 必填——条件满足且未填/未选时提示 | 必填——条件满足且填了但不符合约束时提示 |

#### 4.5.3 明细铁律（不为 `无。` 时）

- **一字段一行**，`字段名称` 列 = 字段规范具体字段名；**禁**汇总占位（「其他字段」「以原型为准」「以 IMG-xx 为准」「同上」「若干字段」等）、**禁**未落具体名的泛化写法。
- `未填写/未选择提示` 列：
  - 文本类填写场景 → `请输入<字段名>`
  - 选择类场景（下拉/单选/复选/级联）→ `请选择<字段名>`
  - `<字段名>` 与 `字段名称` 列文字一字不差
  - 非必填字段填 `-`
- `输入错误提示` 列：**仅写错误提示语本身**（即用户看到的文案），对应**字段规范的约束规则**逐条触发：
  - 格式错误：`<字段名>格式不合法，请检查 <具体格式要求>`
  - 长度超限：`<字段名>长度超出限制（X~Y 字符）`
  - 唯一性冲突：`<字段名>已存在，请重新输入`
  - 范围越界：`<字段名>超出允许范围（X~Y）`
  - 枚举不合法：`<字段名>不在合法枚举内，请重新选择`
  - 防注入命中：`<字段名>包含非法字符，请检查`
  - 多条约束 → 用 `；` 分隔
  - **严禁混入校验逻辑包装**——禁止出现 `失焦或提交时若...` / `则拦截保存` / `则拦截并` / `系统不落库` / `仅在<X>时为必填` 等校验逻辑/触发条件/失败后行为描述。**列 3 = 纯提示语，不是校验规则**。
  - ❌ `失焦或提交时若所选项不在「实物商品 / 虚拟商品」枚举内，则拦截保存并提示「商品类型不合法，请重新选择」，系统不落库。`
  - ✅ `商品类型不合法，请重新选择`
  - **禁止**整段泛化，不得整段套用"按字段规范校验"等推托话术

#### 4.5.4 与字段规范对齐

- 凡字段规范中 `必填=是` 或 `必填=条件必填` 字段 → 本表**必现**（两列均要填）
- 凡字段规范中 `必填=否` 但有约束规则 → 本表**必现**（仅填列 3 输入错误提示，列 2 填 `-`）
- 凡字段规范中 `必填=否` 且**无任何约束** → 本表可不出现该字段
- 无法枚举时先入「待确认项」，不得用泛化表代替

#### 4.5.5 适用范围（按功能点类型严格判定）

**仅以下 4 类功能点需要写提示消息字段表，其它功能点一律写 `无。`**：

| 功能点类型 | 提示消息 | 说明 |
|---|---|---|
| **新增** / 新增-X / 新增子分类 等所有新增变体 | **字段表** | 用户输入/选择字段并保存 |
| **编辑** | **字段表** | 用户修改字段值并保存 |
| **改价格** / 改库存 / 改某字段值 类 | **字段表** | 本质是编辑某字段并保存 |
| **设置 / 配置类**（如设置默认 / 配置规则 / 维护参数）| **字段表** | 用户填表 + 保存 |

**其它所有功能点一律写 `无。`**：

| 功能点 | 校验提示去哪 |
|---|---|
| 查询 / 查看 | 无校验需求 |
| 删除 / 启用 / 停用 / 上架 / 下架 / 关闭 / 签收 / 收货 等状态变更 | "请先选择数据"等 → §4.4 校验规则（业务逻辑类）|
| 发布到门店 / 发布类 | "请选择商品""请选择门店"等 → §4.4 校验规则 |
| 导入 | "请选择文件""文件格式错误""导入超过上限"等 → §4.4 校验规则 |
| 导出 | "数据量超限""无导出权限"等 → §4.4 校验规则 |
| 打印类 | "请选择打印机""份数必须 ≥1"等 → §4.4 校验规则 |
| 复制 | "复制冲突""副本名称重复"等 → §4.4 校验规则 |

**判定铁律**：
- 提示消息字段表**仅承载"表单字段级"红字提示**（用户在字段下方看到的）。
- 任何"toast / 弹窗 / 系统消息"形式的校验提示 → 一律进 §4.4 校验规则。
- 即使某动作有"选择字段"（如打印选打印机、发布选门店），只要不是"填表 + 保存"主流程，提示消息仍写 `无。`，相关校验进 §4.4。
- 不得为了"丰富 PRD"而强加提示消息字段表到非 4 类功能点。

**差异升级铁律（遇边界场景必须先问用户）**：

当 AI 处理截图时遇到以下情况，**禁止**自行决定按 4 类规则归类或自行扩展，**必须先在对话框告知用户并请其判断**：

| 场景 | AI 必做动作 |
|---|---|
| 截图功能点**名称不属于 4 类**（如某"维护"/"调整"/"配置参数"功能），但截图显示有用户填表 + 保存动作 | 在对话框输出："识别到 `<功能点名>` 截图含用户输入字段 + 保存动作，按当前规则应归 4 类之一，但功能点名称不在白名单。请确认：① 按 `<类型>` 处理（写字段表）② 仍写 `无。` ③ 其它处理方式" |
| 截图功能点**属于"其它"类**（删除/启用/打印 等），但截图显示有字段下方红字提示文案 | 在对话框输出："识别到 `<功能点名>` 截图字段下方有红字提示 `<提示文案>`，但按当前规则该功能点应写 `无。`（校验进 §4.4）。请确认：① 仍按规则写 `无。`，校验进 §4.4 ② 破例为该功能点加字段表 ③ 其它处理" |
| 截图特征模糊 / 与规则边界不明 | 同上——先询问，不自作主张 |

**禁止行为**：
- 静默扩展 4 类（如把"打印"自动当编辑类，输出字段表）
- 静默归"无。"（明明截图有红字提示却忽略）
- 自创第 5 类（如"特殊维护类"）

**用户判断后处理**：
- 用户确认"按 4 类处理" → 按对应类型输出字段表
- 用户确认"仍写 `无。`" → 字段下方红字提示移到 §4.4 校验规则
- 用户确认"破例加字段表" → 输出字段表，同时记录为该项目的特殊例外（可考虑后续是否升级规则）

#### 4.5.7 触发时机（适用于 4 类有字段表的功能点）

`保存 / 提交 / 继续 / 下一步` 等动作中：
- 必填项/必选项未填写或未选择 → 列 2 触发
- 任何字段输入内容不符合系统约束 → 列 3 触发

#### 4.5.8 标准示范案例（供应商基本信息表单）

**截图特征**：

```
* 供应商名称：[请输入供应商名称]   ← 红 *、文本输入框
              └ 红字：请输入供应商名称（用户未填时触发）

  供应商编码：[张总            ]   ← 无 *、文本输入框
              └ 红字：供应商编码只能包含英文、数字下划线（用户输入"张总"违反格式时触发）

  供应商分类：[无 ▼]  新建分类 | 刷新   ← 无 *、下拉选择（支持新建分类）
              └ 灰字 helper text：供应商分类用于采购管理与财务统计

  物流模式：[请选择 ▼]   ← 无 *、下拉选择

  供应商地址：[请选择省 / 市 / 区 ▼]   ← 无 *、级联选择三级
              [填写详细地址          ]  ← 无 *、文本输入框（textarea）
```

**字段规范表**：

| 字段名称 | 类型 | 是否必填/必选 | 默认值 | 约束规则 |
| --- | --- | --- | --- | --- |
| 供应商名称 | 文本输入框 | 是 | 无 | 不能为空；长度建议 1~100 字符 |
| 供应商编码 | 文本输入框 | 否 | 无 | 只能包含英文、数字、下划线；不可重复；长度建议 1~50 字符 |
| 供应商分类 | 下拉选择（支持新建分类）| 否 | "无" | 可从已有分类中选择，或通过"新建分类"添加；用于采购管理与财务统计 |
| 物流模式 | 下拉选择 | 否 | 请选择 | 从预设的物流模式选项中选择 |
| 省/市/区 | 级联选择（省市区三级）| 否 | 请选择省/市/区 | 必须选择到具体的区/县级别 |
| 详细地址 | 文本输入框 | 否 | 无 | 长度建议 1~200 字符 |

**提示消息表**：

| 字段名称 | 未填写/未选择提示 | 输入错误提示 |
| --- | --- | --- |
| 供应商名称 | 请输入供应商名称 | 供应商名称长度超出限制（1~100 字符）|
| 供应商编码 | - | 供应商编码只能包含英文、数字、下划线；长度超出限制（1~50 字符）|
| 详细地址 | - | 详细地址长度超出限制（1~200 字符）|

**判定要点（AI 须按此判别）**：

| 字段 | 列 2 判定 | 列 3 判定 | 是否进表 |
|---|---|---|---|
| 供应商名称（必填=是 + 文本输入框）| `请输入供应商名称` | 按行业兜底（名称类长度）| ✅ 必现 |
| 供应商编码（必填=否 + 文本输入框 + 有格式约束）| `-` | 按行业（编码类格式）| ✅ 必现 |
| 供应商分类（必填=否 + 纯选择类）| `-` | `-`（选择类无输入错误可能）| ❌ 不进表 |
| 物流模式（必填=否 + 纯选择类）| `-` | `-` | ❌ 不进表 |
| 省/市/区（必填=否 + 纯级联选择）| `-` | `-` | ❌ 不进表 |
| 详细地址（必填=否 + 文本输入框 + 有长度约束）| `-` | 按行业（地址类长度）| ✅ 必现 |

**铁律总结**：

1. 文本类字段（文本输入框 / 数值输入框 / 金额输入框 / 日期 / 上传 等用户键入）→ 进表
2. 纯选择类字段（下拉选择 / 单选 / 复选 / 级联 / 树形 等用户选择）+ **必填=否** → 不进表
3. 纯选择类字段 + **必填=是** → 进表（列 2 写 `请选择<字段名>`，列 3 写 `-`，因为选择枚举无错误可能）
4. 截图中已有红字提示 → AI 应**优先采用截图实际文案**，未明示时按行业兜底

### 4.6 消息通知

**定义**：系统主动推送给用户的**站内信通知**（与"提示消息"/"操作日志"职责分离）：
- 提示消息：页面字段红字，给操作人**当下同步**看
- 操作日志：审计留痕，不主动推送
- **消息通知**：异步推送的站内信通知，按"两类接收人规则"投递

#### 4.6.1 接收人两类规则（核心铁律）

**接收人 = 与该单据/事项有「所属或利益关系」的角色**，分两类：

**第 1 类｜自我通知**：
- **触发场景**：你创建/提交的单据或事项发生状态变化（成功/失败/异常/审核结果）
- **接收人** = 创建人 / 提交人本身
- **典型例子**：
  - 你的拆箱单 CX358 在 `<时间>` 创建成功（→ 拆箱单创建人）
  - 你提交的销售订单号：SO123 在 `<时间>` 截单失败（→ 订单提交人）
  - 你提交的销售订单号：SO123 在 `<时间>` 标记销售订单异常（→ 订单提交人）

**第 2 类｜他人触发的通知**：
- **触发场景**：别人对你的单据/事项执行了操作，或业务流转影响了你
- **接收人** = 受影响的相关方（**非操作人**）
- **典型例子**：
  - 总部发布商品 → 门店店长收
  - 审核人通过/驳回申请 → 申请人收
  - 仓管发货你的订单 → 客户收

#### 4.6.2 接收人通用规则

- 接收角色**必须具体化**（如`创建人` / `提交人` / `门店店长` / `审核人` / `仓管员`），**禁止**泛指"用户" / "相关人员"。
- 接收人必须能从消息中获得"新信息"或"行动指引"。
- 操作人若不是单据归属人、且操作结果不影响他工作 → 不发。
- ~~"接收人非操作人"已废止~~：原铁律仅适用同步即时操作，与"自我通知"场景冲突。新规则按两类判定。

#### 4.6.3 出表 / 不出表的判定铁律

**AI 默认不猜测、不自创消息通知**，**严格按用户在 prompt「消息通知清单」中列出的正面清单处理**：

| 用户 prompt 状态 | AI 行为 |
|---|---|
| 在清单中列出了"功能点 X" | AI 在该功能点输出消息通知字段表（按 §4.6.5 35 类标准模板自动套用接收人/标题/内容）|
| 未在清单中列出 | 该功能点写 `• 消息通知：无。` |
| 截图明确显示某操作有系统通知 | 仍按用户 prompt 为准；若 prompt 未列出，进「待确认项」请用户确认 |

**清单填写格式**（用完整菜单路径 + 功能点名精确指定）：

```
9) 消息通知清单：
- <系统视图>-<系统菜单>-<末级菜单>-<功能点名>
- ...
```

**示例**：

```
- 连锁门店系统-商品-商品库-发布到门店
- 连锁门店系统-商品-商品库-上架
- 连锁门店系统-商品-门店商品-上架
```

**填写要点**：

- **不写编号**（不写 `4.4.1.1.8` 这种数字）——用菜单路径而非编号定位。
- **不写"是/否"**——出现在清单里即表示要发。
- **不写接收人/标题/内容/渠道**——AI 按 §4.6.5 35 类标准模板自动套用。
- **完整菜单路径精确定位**——同名功能点在不同菜单下时（如商品库.上架 vs 门店商品.上架），用菜单路径精确区分，AI 按"末级菜单 + 功能点名"匹配模板：
  - 商品库下「上架」（总部下发）→ 双消息（总部商品运营 + 受影响门店店员）
  - 门店商品下「上架」（门店本店）→ 单消息（同店其他角色）
- 如某功能点需差异化覆盖模板默认值，可在该行末追加 `备注：<说明>`（如"备注：接收人改为'省区督导'"）。

**AI 匹配铁律**：

- 严格按"末级菜单 + 功能点名"匹配 §4.6.5 模板，不靠猜测。
- 用户清单中的菜单路径必须与 PRD 实际菜单结构一致；若菜单路径不匹配（如拼写错误、菜单不存在）→ 进「待确认项」请用户更正，**不自创**。
- 功能点名不在 §4.6.5 35 类模板中时（自定义业务动作）→ 进「待确认项」请用户补充模板，**不自创**。
- 仅列出在清单中的功能点输出消息通知；其余一律 `• 消息通知：无。`。

**禁止行为**：
- ❌ AI 自作主张为某功能点添加消息通知（即便业务直觉认为需要）
- ❌ 把"提示消息"或"操作日志"的内容混入消息通知
- ❌ 接收人写成"用户" / "相关人员" 等泛指

#### 4.6.4 字段表结构

**格式真理源**：消息通知统一用 **4 列固定表头 `通知场景 | 通知标题 | 通知内容 | 接收方`**（与标注层 [annotation-templates.md「消息通知」唯一格式真理源] 一致、与原型渲染一致）；动态字段用 `{占位符}`（非 `<>`）；接收方写具体角色（禁"用户/相关人员"泛指）。**禁旧 3 列「字段/字段说明/规则示例」**。机器版 = `prd-content-lint.js` ⑧ 硬拦非 4 列。

**单消息**（无跨角色协同场景，仅给操作人 / 创建人 / 系统受影响方之一）：

```
• 消息通知：

| 通知场景 | 通知标题 | 通知内容 | 接收方 |
|---|---|---|---|
| <触发该通知的业务动作/状态> | <通知卡片标题，简短> | <通知正文，动态字段用 {占位符}，禁写死示例值> | <具体接收方角色> |
```

**双消息**（跨角色协同场景，须同时通知操作人 + 受影响方）：

采用「中文小标题分块」结构，**两张独立表**：

```
• 消息通知：

【消息 1：操作人确认】

| 通知场景 | 通知标题 | 通知内容 | 接收方 |
|---|---|---|---|
| <操作完成场景> | <标题模板> | <内容模板，动态字段用 {占位符}> | <操作人具体角色> |

【消息 2:受影响方通知】

| 通知场景 | 通知标题 | 通知内容 | 接收方 |
|---|---|---|---|
| <受影响方业务场景> | <标题模板> | <内容模板，动态字段用 {占位符}> | <受影响方具体角色> |
```

**双消息适用范围**（共 8 类，强制双消息）：

| 动作类型 | 消息 1 接收人 | 消息 2 接收人 |
|---|---|---|
| 发布到门店 | 总部商品运营 | 受影响门店店长/店员 |
| 上架（总部）| 总部商品运营 | 受影响门店店员 |
| 下架（总部）| 总部商品运营 | 受影响门店店员 |
| 改价格（总部）| 总部商品运营 | 受影响门店店长 |
| 订单发货 | 仓库管理员 | 订单客户 |
| 订单签收 | 订单客户 | 总部 |
| 退货申请 | 退货申请人（客户）| 仓库管理员 |
| 退货完成 | 仓库管理员 | 订单客户 |

其它 27 类均为单消息。

**全局风格规范**：

| 维度 | 规则 |
|---|---|
| 时间格式 | `YYYY-MM-DD HH:mm:ss`（如 `2025-11-04 16:00:00`）|
| 时间介词 | `在 <时间>`（统一不用"已于"）|
| 第二人称 | 创建/所属类用 `你的`；操作动作类用 `你提交的` |
| 单据标识 | 创建类：`<单据全称> <单号>`；操作类：`<单据全称>号：<编号>` |
| 批量量词 | 批量场景显式带 `<N> 笔/个` |
| 失败类要素 | 原因 + 下一步动作 |
| 异常类要素 | 原因 + 紧迫度（"请于 X 小时内处理"）|
| 末尾指引 | 必含 `请在「<模块>」查看/处理` 跳转入口 |
| 单据全称 | 销售订单 / 采购订单 / 退货订单 / 拆箱单 / 入库单 / 出库单 等（禁简称）|
| 通知渠道 | 站内信（默认且唯一）|
| 发送人 | 不显示在表格 |

#### 4.6.5 标准消息通知模板库（35 类）

**一、业务下发类（4 类，双消息）**

| 动作 | 消息 | 接收人 | 标题 | 内容模板 |
|---|---|---|---|---|
| 发布到门店 | 消息 1 | 总部商品运营 | 发布到门店成功 | 你提交的 `<N>` 个商品 在 `<时间>` 发布到门店成功，请在「商品库」查看 |
| 发布到门店 | 消息 2 | 受影响门店店长/店员 | 商品发布到门店 | 总部 在 `<时间>` 发布 `<N>` 个商品到本店，请在「门店商品」查看 |
| 上架（总部）| 消息 1 | 总部商品运营 | 上架成功 | 你提交的 `<N>` 笔商品 在 `<时间>` 上架成功，请在「商品库」查看 |
| 上架（总部）| 消息 2 | 受影响门店店员 | 商品上架通知 | 总部 在 `<时间>` 完成 `<N>` 个商品上架，状态变为销售中，请在「门店商品」查看 |
| 下架（总部）| 消息 1 | 总部商品运营 | 下架成功 | 你提交的 `<N>` 笔商品 在 `<时间>` 下架成功，请在「商品库」查看 |
| 下架（总部）| 消息 2 | 受影响门店店员 | 商品下架通知 | 总部 在 `<时间>` 完成 `<N>` 个商品下架，请在「门店商品」核对状态 |
| 改价格（总部）| 消息 1 | 总部商品运营 | 改价成功 | 你提交的 `<N>` 笔商品改价 在 `<时间>` 完成，请在「商品库」查看 |
| 改价格（总部）| 消息 2 | 受影响门店店长 | 商品价格调整 | 总部 在 `<时间>` 调整 `<N>` 个商品售价，请在「门店商品」核对价签 |

**二、单据流转类（7 类，第 1 类接收人 + 异常类附加业务负责人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 单据创建成功 | 创建人 | `<单据全称>`创建成功 | 你的`<单据全称>` `<单号>` 在 `<时间>` 创建成功，请在「`<模块>`」查看 |
| 单据动作成功（单条）| 提交人 | `<动作>`成功 | 你提交的`<单据全称>`号：`<编号>` 在 `<时间>` `<动作>`成功，请在「`<模块>`」查看 |
| 单据动作成功（批量）| 提交人 | `<动作>`成功 | 你提交的 `<N>` 笔`<单据全称>` 在 `<时间>` `<动作>`成功，请在「`<模块>`」查看 |
| 单据动作失败 | 提交人 | `<动作>`失败 | 你提交的`<单据全称>`号：`<编号>` 在 `<时间>` `<动作>`失败，原因：`<原因>`，请在「`<模块>`」中重新发起 |
| 单据标记异常 | 提交人 + 业务负责人 | `<单据全称>`异常 | 你提交的`<单据全称>`号：`<编号>` 在 `<时间>` 标记`<单据全称>`异常；异常原因：`<原因>`，请于 `<X>` 小时内在「异常处理」中处理 |
| 单据审核通过 | 提交人 | `<单据全称>`审核通过 | 你提交的`<单据全称>`号：`<编号>` 在 `<时间>` 审核通过，请在「`<模块>`」查看 |
| 单据审核驳回 | 提交人 | `<单据全称>`审核驳回 | 你提交的`<单据全称>`号：`<编号>` 在 `<时间>` 审核未通过；驳回原因：`<原因>`，请在「`<模块>`」中修改后重新提交 |

**三、订单流转类（4 类，双消息）**

| 动作 | 消息 | 接收人 | 标题 | 内容模板 |
|---|---|---|---|---|
| 订单发货 | 消息 1 | 仓库管理员 | 销售订单发货成功 | 你提交的销售订单号：`<编号>` 在 `<时间>` 发货成功，物流单号 `<X>`，请在「订单管理」查看 |
| 订单发货 | 消息 2 | 订单客户 | 销售订单已发货 | 你的销售订单 `<单号>` 在 `<时间>` 发货，物流单号 `<X>`，请在「我的订单」查看 |
| 订单签收 | 消息 1 | 订单客户 | 销售订单已签收 | 你的销售订单 `<单号>` 在 `<时间>` 已签收，请在「我的订单」查看 |
| 订单签收 | 消息 2 | 总部 | 销售订单已签收 | 销售订单 `<单号>` 在 `<时间>` 由客户签收，请在「订单管理」查看 |
| 退货申请 | 消息 1 | 退货申请人（客户）| 退货申请已提交 | 你提交的退货订单号：`<编号>` 在 `<时间>` 提交成功，请在「我的退货」查看进度 |
| 退货申请 | 消息 2 | 仓库管理员 | 待处理：退货申请 | `<申请人>` 在 `<时间>` 提交退货订单号：`<编号>`，请于 24 小时内在「退货管理」中审核处理 |
| 退货完成 | 消息 1 | 仓库管理员 | 退货处理完成 | 你提交的退货订单号：`<编号>` 在 `<时间>` 完成退货，请在「退货管理」查看 |
| 退货完成 | 消息 2 | 订单客户 | 退货已完成 | 你的退货订单 `<单号>` 在 `<时间>` 完成，请在「我的订单」查看 |

**四、审批流类（3 类，第 2 类接收人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 审批提交 | 审核人 | 待审核：`<申请类型>` | `<提交人>` 在 `<时间>` 提交 `<申请类型>`，请于 `<X>` 小时内在「待办审批」中处理 |
| 审批通过 | 申请人 | 申请已通过 | 你于 `<提交时间>` 提交的 `<申请类型>` 在 `<时间>` 审核通过，请在「我的申请」查看 |
| 审批驳回 | 申请人 | 申请已驳回 | 你于 `<提交时间>` 提交的 `<申请类型>` 在 `<时间>` 审核未通过；驳回原因：`<原因>`，请在「我的申请」中修改后重新提交 |

**五、预警告警类（4 类，第 2 类接收人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 库存预警 | 采购员、仓管员 | 库存预警：`<商品>` | 商品 `<编号>` 库存 `<X>`，达到预警阈值，请于 24 小时内在「库存管理」中处理 |
| 到期预警 | 仓管员、运营 | 商品到期预警 | 商品 `<编号>` 距到期 `<X>` 天，请在「商品库」中处理 |
| 异常告警 | 运维、业务负责人 | 异常告警：`<场景>` | 在 `<时间>` 检测到异常 `<描述>`，请于 2 小时内在「异常监控」中处理 |
| 数据同步异常 | 运维 | 数据同步异常 | 在 `<时间>` 检测到数据同步异常：`<描述>`，请在「数据监控」中处理 |

**六、用户/权限类（2 类，第 2 类接收人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 用户禁用 | 被禁用用户 | 账号已禁用 | 你的账号 在 `<时间>` 被禁用，原因：`<原因>`，如有疑问请联系管理员 |
| 权限变更 | 受影响用户 | 权限已调整 | 你的角色/权限 在 `<时间>` 调整为 `<新角色>`，请重新登录后查看 |

**七、协作类（1 类，第 2 类接收人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 任务分派 | 被指派人 | 新任务：`<任务名>` | 你被指派 `<任务名>`，截止 `<时间>`，请在「我的任务」中处理 |

**八、财务/账户类（4 类）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 充值成功 | 充值账号所有人（第 1 类）| 充值成功 | 你的账户 在 `<时间>` 充值 `<金额>`，当前余额 `<X>`，请在「账户余额」查看 |
| 余额不足 | 账户管理员、财务（第 2 类）| 账户余额不足 | 账户 `<名称>` 余额仅剩 `<X>`，请于 24 小时内在「充值管理」中处理 |
| 账单生成 | 受账单人（第 2 类）| 新账单已生成 | 你的 `<期间>` 账单 在 `<时间>` 生成，金额 `<X>`，请在「账单管理」查看 |
| 结算异常 | 财务、业务负责人（第 2 类）| 结算异常：`<原因>` | 在 `<时间>` 检测到结算异常，原因：`<原因>`，请于 8 小时内在「结算管理」中处理 |

**九、仓储类（2 类，第 2 类接收人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 盘点完成 | 运营、仓管员 | 盘点已完成 | `<门店>` 在 `<时间>` 完成盘点，差异 `<X>` 条，请在「盘点管理」查看 |
| 采购入库 | 仓管员、采购员 | 采购入库完成 | 采购单 `<单号>` 在 `<时间>` 入库，数量 `<X>`，请在「入库管理」查看 |

**十、营销类（2 类，第 2 类接收人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 促销开始 | 受影响门店 | 促销活动开始 | 促销 `<名称>` 在 `<时间>` 开始，将持续至 `<截止>`，请在「促销管理」查看 |
| 促销结束 | 受影响门店 | 促销活动结束 | 促销 `<名称>` 在 `<时间>` 结束，请在「促销管理」查看 |

**十一、会员类（2 类，第 2 类接收人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 会员升级 | 会员本人 | 会员等级升级 | 你的会员等级 在 `<时间>` 升级为 `<等级>`，享 `<权益>`，请在「我的会员」查看 |
| 会员到期预警 | 会员本人 | 会员即将到期 | 你的会员 `<等级>` 将于 `<时间>` 到期，请在「我的会员」中续费 |

**十二、系统类（1 类，第 2 类接收人）**

| 动作 | 接收人 | 消息标题 | 消息内容模板 |
|---|---|---|---|
| 系统维护 | 全体用户 | 系统维护通知 | 系统将于 `<时间>` 进行维护，预计耗时 `<时长>`，请提前保存数据 |

#### 4.6.6 紧迫度量化参考

| 场景 | 紧迫度 |
|---|---|
| 库存预警 / 余额不足 | 24 小时内 |
| 单据异常 | 4 小时内 |
| 异常告警 / 数据同步异常 | 2 小时内 |
| 结算异常 | 8 小时内 |

数字可按业务实际调整，但应明确写出，禁止泛指"尽快"。

### 4.7 操作日志（显示铁律）

凡功能点需要输出 `操作日志`，必须按以下结构（任何偏离均为违规）：

1. 先写固定说明语（一字不易）：
   `用户操作成功后，系统在操作日志模块记录：操作时间、操作账号、操作模块、操作功能、操作明细、IP地址。`
2. 紧跟 1 个 Markdown 表，表头固定为 `| 字段 | 字段说明 | 规则/示例 |`。
3. 固定 6 行且锁定行序：`操作时间`、`操作账号`、`操作模块`、`操作功能`、`操作明细`、`IP地址`。

严禁：只写表格不写说明语、只写说明语不写表格、改列名/列序/增删列、增删行或调整行序。

**`查询` / `查看` 一律不输出本小节**（说明语与表格都不出）。

#### 4.7.1 操作明细字段通用规则

**格式**：`<动作><对象类型>：<对象唯一标识>=<值>[，<动作详情>]`（参考 SAP / Oracle EBS / Dynamics 365 / Salesforce 审计日志格式）

**组成**：

| 部分 | 填法 |
|---|---|
| `<动作>` | 取自「操作功能」列的动作名 |
| `<对象类型>` | 业务对象名（商品 / 凭证 / 订单 / 库位 等），不带"对象"二字 |
| `<对象唯一标识>`（必含）| 优先编号 + 辅以名称；无编号时用关键业务字段 |
| `<动作详情>` | 按动作类型选填 |

**动作详情按类型填写**：

| 类型 | 动作详情 |
|---|---|
| 编辑 | `<字段>："<旧>"→"<新>"` |
| 改价 | `原价 ¥X → 新价 ¥Y` |
| 范围类（发布到门店 / 指定门店上下架）| `范围=<>` 或 `目标门店=[<>]` |
| 批量 | `共 N 件，对象列表=[...]`（>10 时列前 3 + "...等 N 个"）|
| 导入 | `成功 X 条，失败 Y 条，文件名=<>` |
| 导出 | `N 条，范围=<>，格式=<>` |
| 复制 | `源=<>，新生成=<>，复制范围=[<>]` |
| 打印 | `N 件，模板=<>` |
| 审核驳回 | `驳回原因=<>` |
| 状态变更（启用/停用/上下架）| 隐含在动作名中，可省略 |

**填写示例**（每类挑代表性的，其它按同样模式扩展）：

| 场景 | 示例 |
|---|---|
| 新增 | `新增商品：编号=SPU001234，名称=可口可乐 330ml` |
| 编辑 | `编辑商品：编号=SPU001234，售价：¥3.5→¥4.0` |
| 删除 | `删除商品：编号=SPU001234，名称=可口可乐 330ml` |
| 上架（范围）| `上架商品：编号=SPU001234，范围=全店` |
| 改价 | `改价：编号=SPU001234，原价 ¥3.5 → 新价 ¥4.0` |
| 发布到门店 | `发布到门店：商品=SPU001234，目标门店=[北京三里屯店,上海徐汇店]，发布模式=立即` |
| 导入 | `导入商品：成功 50 条，失败 2 条，文件名=商品导入_20260525.xlsx` |
| 批量编辑 | `批量编辑商品：共 20 件，修改字段=[分类,品牌]` |
| 审核驳回 | `审核驳回：商品=SPU001234，驳回原因=规格信息不全` |
| 商城下单 | `下单：订单号=SO20260525001，商品=[SKU001×2,SKU002×1]，金额=¥80.50` |

**4 条强制约束**：

1. 必含对象唯一标识（禁止 `修改成功` / `批量操作了 N 条` 类无标识描述）
2. 编辑类必含旧值 → 新值
3. 批量类必含统计（`共 N 件` 或 `成功 X 条，失败 Y 条`）
4. 禁含敏感信息（密码 / Token / 身份证 / 银行卡 / 客户手机号脱敏）

#### 4.7.2 适用范围细化（C 端商城前台特殊判定）

| 场景 | 是否输出操作日志 | 理由 |
|---|---|---|
| 查询 / 查看 | ❌ 不输出 | 不改变核心数据（已有规则）|
| **C 端商城前台-交易类**（下单/支付/退款/修改账号/修改密码/修改收货地址）| ✅ **必输出**，套用 §4.7.1 通用规则 | 涉及金钱/账号安全，PCI DSS、个保法、金融监管要求 |
| **C 端商城前台-浏览类**（浏览/搜索/筛选/收藏/加入购物车）| ❌ **不输出** | 不改变核心数据；由独立的「用户行为埋点」承载（不在 PRD 操作日志范畴）|
| 其它（新增/编辑/启用/停用/上架/下架/改价/审核/导入/导出/发货/收货 等）| ✅ 必输出 | 已有规则 |

**关键判定原则**：是否改变**核心业务数据**（金钱流转、账号状态、订单状态、库存等）。是 → 输出；否 → 不输出。

### 4.8 留痕动作范围

除 `查询 / 查看` 外，其他需留痕动作（新增、编辑、启用、停用、删除、关闭、设置、保存、发货、标记发货、签收、收货、上架 等）必须记录操作日志。

---

## 5 二次确认

- `查询 / 查看` 不触发二次确认。
- 其他动作按动作类型输出文案（详见 [confirmation-dialog.md](../references/standards/confirmation-dialog.md)）：
  - 新增、编辑（单条）：`系统弹出二次确认提示框：确认提交该<业务名称>？`
  - 启用、停用、关闭、发货、标记发货、删除：单条 `确认<功能名称>该<业务名称>？` / 多条 `确认<功能名称>所选<业务名称>？`
  - 签收、收货、上架：`确认<按钮名称>该<业务名称>？`
  - 发布、改价、导入、复制 等：见配套块。
- `<业务名称>` 取当前操作对象（商品、订单、出库订单、退货单、库区、仓库、账号 等），按实际替换，不得保留占位符。
- 同动作支持单/多条时分别写文案；仅单条时不得写多条文案。

---

## 6 动作类补齐规则

除 `新增 / 编辑 / 查询 / 查看` 外，其他功能点必须补齐对应类型的下列要点：

| 动作类 | 必须明确 |
|---|---|
| 状态变更（启用/停用/上下架/发布） | 状态流转（允许前置态、禁止态、执行后态）；批量场景的部分失败口径 |
| 删除 | 软/硬删除；引用校验拦截；可恢复策略；批量结果统计口径 |
| 改价 | 生效范围（单门店/多门店/全渠道）；生效时机（即时/定时）；价格精度与币种；最低价保护 |
| 导入 | 模板版本；字段映射；行级校验；错误明细回传；是否允许「仅导入合法行」 |
| 导出 | 导出字段；脱敏规则；最大导出量；异步任务与下载有效期；导出权限与留痕 |
| 打印（价签/条码） | 模板来源与版本；份数限制；失败重试与补打 |
| 复制 | 复制范围（复制/不复制字段）；冲突处理（重名/编码）；复制后默认状态 |
| 批量操作 | 选择上限；跨页选择规则；执行前校验；执行后结果回执（成功/失败/原因） |
| 异步任务 | 状态机（排队/执行中/成功/失败/部分成功）；取消策略；通知方式 |
| 权限审计 | 可见/可用/可执行差异；操作日志记录关键审计字段（对象标识、关键前后值、IP、终端） |

---

## 7 图片优先

详见 [image-recognition.md](../references/standards/image-recognition.md)（识别清单、反模式、待确认项、合理化建议、多图绑定、输出顺序）。

要点：
- 有截图：字段骨架从图提取；唯一性 / 业务校验 / 默认值业务含义 / 错误文案 / 字段级触发时机/权限 等图片看不到的信息从文字补充取，无文字补充则进「待确认项」，不得臆测。
- 占位提示（灰字 `请输入...` `--请选择--`）不算默认值。
- 图片与文字冲突一律进「待确认项」，等待澄清。
- 多图或难对齐场景须先输出「图片绑定表」，模板见配套块第二节。

### 7.1 输出归属铁律（识别 5 段不进 PRD 文件）

**图片绑定表 / 图片识别结果 / 图片归档清单 / 待确认项 / 合理化建议** 这五段属于 **AI 与用户的对话辅助输出**，只在对话消息中呈现；**严禁**写入 PRD 正文文件（.md 归档稿）。

具体禁止：
- **禁止**在 PRD 文件出现 `### 本批截图识别摘要(...)` 类标题及其下任何识别表格。
- **禁止**把 `**图片绑定表**` / `**图片归档清单（固定五段）**` / `**待确认项**` / `**合理化建议**` 作为加粗段落写入 PRD 文件。
- **禁止**把"图片编号 - 系统视图 - 菜单路径 - 功能点 - 图片用途"归档清单作为附录写进 PRD。
- PRD 文件中引用图片**仅**通过功能点的 `<功能点编号>.2 原型图` 写 `IMG-xx` 编号即可；识别细节由对话和归档清单（在对话中输出）承载。

判定标准：PRD .md 文件中出现以上 5 段任一形式 = 违规，须删除后重新输出。

---

## 8 「请参考」与「完整不合并」

### 8.1 请参考（分小节判定）

`字段规范` 与 `用例规则` 是否允许写「请参考」**分别**按「与对照功能点对应小节是否 100% 一致」判定（**禁**以"整体差不多"代替小节级判定）。引用句统一为单行、顶格：

`请参考：<对照功能点编号> <对照功能点名称>`

四种情形：

| 情形 | 字段规范 | 用例规则 |
|---|---|---|
| 两小节均 100% 一致 | **省略**本小节（子项顺延为 .3 用例规则） | 仅一行 `请参考：…`，不重复七项 |
| 仅字段规范 100% 一致 | 保留 `.3 字段规范`，内仅一行 `请参考：…` | 完整展开七项 |
| 仅用例规则 100% 一致 | 完整展开 | 仅一行 `请参考：…` |
| 任一小节有差异 | 该小节完整展开 | 该小节完整展开 |

判定边界包含按钮、状态、文案、权限、操作日志、原型图、二次确认、校验规则等的等价性。

### 8.2 完整不合并模式

用户明确要求「完整版 / 完整展示 / 对话框全文 / 即使相同内容也不要合并 / 不得合并 / 不要合并」等口径时：

- PRD 功能点明细须**逐菜单、逐功能点**写全所有子节与表格。
- **禁止**：同上、参见、以下略、表格同上、合并展示、复用前文代替、从略 等任何形式的合并或省略。
- 内容逐字相同也须在**每个**功能点下完整重写。
- 此模式下**不适用** §8.1 单行「请参考」省略，直至用户明确允许引用。
- 单次装不下分**多条连续消息**续写，禁止以合并代替未输出部分。

---

## 9 输出质量要求

- 只写真实操作，不使用 `N/A` 占位；"本次不需要 / 本次没有" 统一改为 `无`。
- 验收语句必须可验证，避免「体验更好」「尽量」「适当」等模糊词。
- 信息缺失时先补最少必要问题再输出正文；不得擅自猜测。
- 文案优先短句，避免长段落绕述与术语堆砌；术语首次出现须就地解释。
- PRD 正文不写「元说明」：`####` 菜单/功能点标题下**不**用 `•` 写「一级菜单」「功能点」「与下一节区分」等解释性句子；编号 + 标题即层级。
