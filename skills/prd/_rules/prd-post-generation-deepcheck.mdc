---
description: PRD 生成后强制深检铁律——任何 PRD 生成/更新后必对照 prd skill 全部规则原文逐功能点全量深检，严禁抽检、严禁擅自主张；机器闸 prd-structure-lint.js 必绿。适用 AI 画新原型 + 打包后所有用户。
globs: "**/PRD*.md"
alwaysApply: true
---

# PRD 生成后强制深检铁律（最后安全线）

> 🏛 **本文件为"生成后质量门"的单一真理源**，与 `prd-template-structure.mdc`、`prd-directory-numbering.mdc` 并列宪法层。
> 立法背景：曾多次出现"改好一处、另一处又错"的拆东墙补西墙——根因是 PRD 没有真正"按规则生成 + 按规则全量核"，而是靠记忆/模板/抽检。本铁律消灭该根因。
> **适用范围：① AI 自己画新原型生成 PRD；② 打包后任何用户安装使用。两者完全一致，随包发。**

## 〇、根本界定（两个来源严格分工，互不越界）

> 在 AI 对话框里**复制已圈功能清单、生成 PRD** 时（含从原型反推、增量改、全新生成），内容与格式分别来自两个来源，**权威范围互不越界**：

| 来源 | 提供什么（权威范围） | 铁律 |
|---|---|---|
| **原型（含已圈功能清单）** | **业务依据**：真实字段名、字段值、必填/校验、默认值、枚举值、菜单结构、业务流程 | 业务内容**一律以原型真值为准，禁臆造**（机器版 = ⑫ `prd-prototype-field-gate.js`：PRD 写了原型没有的字段名 = 红）|
| **prd skill 全部铁律**（`_rules/*` + `references/*`）| **要求和格式**：4 章结构、字段规范 5 列、用例规则 7 节、操作日志 3 列、提示消息/消息通知格式、编号、标题层级、禁词、分条、二次确认句式… | 格式/要求**一律按铁律，不简化、不自创、不因原型长相改格式**（机器版 = ⑧ `prd-content-lint.js` + ⑪ `prd-structure-lint.js`）|

**关键推论（必须守住）：**
- **原型是"业务真值源"，不是"格式模板"。** 例：原型把"创建人/审核人"合并成一个"人员"列、把日期显示成某格式，PRD 字段规范**仍按 prd skill 的格式逐字段规范化书写**，不照搬原型的列布局/控件排布。
- **冲突时**：业务内容听原型，格式/要求听 prd skill 铁律——两者各管各的，谁都不得越界覆盖对方。
- 即"**说什么**"（字段/值/流程）由原型定，"**怎么写**"（结构/格式/措辞）由 prd 铁律定。

## 一、四步强制门（缺一不可，不绿不交）

> ⚡⚡ **最高原则：自检是 AI 的活，不是用户的活（商业产品不让用户当 QA）。**
> - 生成/更新原型或 PRD 后，AI **必须主动**跑完下列四步自检 + `deliver-gate` 全绿，**绝不要等用户说"再检查一次 / 你检查了吗"**。
> - **"已完成"消息里必须附上 `deliver-gate` 全绿输出**（或 `prd-structure-lint` 退出码 0）作为证据；**不附证据 = 视同未完成、视同违规**，不准说"完成/搞定/可以了"。
> - **用户每追问一次自检 = 一次产品级失败**。次数与深度无需用户每次界定——**本铁律已一次性定死：每次生成必做、深度=全量逐功能点细到标点不抽检**。用户重复指定 = 等于让用户当 QA，本身即违背商业产品标准。

任何 PRD `.md` **生成或更新后**，交付/告知用户"已完成"**之前**，必须依次完成：

1. **全量通读规则原文**（不靠记忆、不靠任何摘要清单）
   - 现读 `_rules/prd-template-structure.mdc` 全文 + `_rules/prd-directory-numbering.mdc` + `references/quality-checklist.md`（64 条）+ 本文件 + 涉及到的 `references/standards/*`。
   - **规则文件是唯一权威**；AI 脑中/历史会话里的版本、任何派生清单都只是衍生品，**不得替代现读原文**。

2. **逐功能点、对照每条规则、全量深检——细到标点**
   - N 个功能点就检 N 个，**每个**功能点的**每个**子节（位置/原型图/字段规范/用例规则）、**每张**表的**每一行每一列**、**每一个**标点（`•` U+2022 顶格 / 全角 `：` / 顿号 `1、2、3、` / `「」` / 表格 `|` 顶格 / 段间空行）都要对着规则核。
   - **严禁抽检**："看几个代表性的就过"是被明令禁止的错误工作方式。

3. **机器闸 `prd-structure-lint.js` 必绿**
   - 运行 `node skills/prd/qa/prd-structure-lint.js <PRD.md>`，**退出码必须 0**。
   - 它编码了所有"能机器判"的硬铁律（编号唯一/章节/标题层级/4 子节/7 节齐/操作日志 3 列表头+分隔+6 行/提示消息列/版本变更 4 列/术语/mermaid/原型图不得全无/禁词/R-A 等）；**红 = 不许交付**。
   - 机器闸是兜底，**不是全部**——它绿只代表机械层合规，判断类仍须第 2 步人工核。

4. **判断类逐条人工核 + 不擅自主张**
   - 机器判不了、需推理的铁律（操作明细必含对象唯一标识、消息接收人具体到角色、默认值推导是否合理、字段约束双层是否齐、查看场景判定、提示消息适用 4 类、二次确认双路径文案、消息通知按 prompt 清单不自创…）必须 AI **逐条对着规则原文人工核**。
   - **不擅自主张**：规则覆盖到的严格照规则原文执行；规则没覆盖 / 有歧义 / 截图或数据看不出的，**停下来问用户，绝不臆测、不简化、不扩展、不自行归类**（与 §3.1.4「禁推托」、§4.5.5「差异升级先问」同源）。

## 二、绝对禁止

- ❌ 凭记忆或派生清单代替"现读规则原文"。
- ❌ 抽检（只核部分功能点 / 部分子节 / 部分标点）。
- ❌ 机器闸未绿就告知用户"已完成"。
- ❌ 对规则有疑义时自行决定（必须问用户）。
- ❌ 拆东墙补西墙：只补用户当下指出的那一处、不做全量回归核。

## 二·补、原型图全自动填充（生成时，不靠人工点击）

PRD 的每个功能点 `.2 原型图` **必须有从原型截取的真实截图**（IMG-xx + 图片文件），仅该点确无原型视图才写「无」。**禁止全为「无」**（机器闸 F2 拦）。

填充方式 = **AI 生成 PRD 时自动跑**，零配置、不要求 PM 手点：
1. 用 `node skills/pm-design/qa/auto-screenshot.js -Html <离线原型.html>`——**不给 -States 时自动读原型内自描述清单 `window.__ANNO_SHOT_MANIFEST__`**，无头 Chrome 按原型深链（`?sys/page/open/pins=0`）逐状态全页截图 → 写进每个功能点 `img` → 重生 PRD。**零 npm 依赖**（只用本机 Chrome/Edge）、零人工点击、零手搓配置。
2. `window.__ANNO_SHOT_MANIFEST__`（一视图一条 `{id,query,fps}`，覆盖全部功能点）由 pm-design 生成原型时**烤进原型**（它本就知道页/弹窗/fp，见 pm-design [prototype-template.md §16.1.1](../../pm-design/prototype-template.md)）。外部/无 manifest 的原型才用 `-States <states.json>` 手给。
3. **截完必须人眼 Read 抽验几张**，确认渲染正确、弹窗已打开（prd 铁律：不侥幸）。
4. 关键坑：**不加 `--virtual-time-budget`**（原型 SSE 持续重连会让 Chrome 挂起无图）；URL 作单个参数传；图片路径用无空格 slug。详见脚本头注释。
5. **前提**：原型支持 URL 深链 + 烤入 manifest——这两条由 pm-design [prototype-template.md §16](../../pm-design/prototype-template.md) 强制，新原型自带；故"画新原型 / 打包用户"截图效果一致。

## 三、与其它规则/工具的关系

- 生成机制（如 anno-server 自动拼装）只负责**机械正确**（编号/标题层级/表格结构/原型图截图）；**业务内容**（字段规范/用例规则 7 节）由 AI 对照规则原文真推理产出。任何地方**禁止**私藏第二套与本宪法冲突的格式逻辑。
- 机器闸 `qa/prd-structure-lint.js` = 本铁律第 3 步的可执行版；`references/quality-checklist.md` = 第 2/4 步人工核的条目清单（但仍以 `_rules/` 原文为准）。
- 与 `prd-content-lint.js`（注入前单 pin 内容格式）互补：本闸查"生成后整篇文档"的结构与成型。
