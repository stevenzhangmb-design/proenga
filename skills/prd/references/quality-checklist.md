# PRD 质量检查清单

## ⚠️ 顶级红色铁律自检（任一未通过 = 必须删除后重写）

- [ ] **R-A 图片识别 5 段绝不进 PRD 文件**：PRD `.md` 中**未**出现 `### 本批截图识别摘要(...)`、`**图片绑定表**`、`**图片归档清单（固定五段）**`、`**待确认项**`、`**合理化建议**` 等任何加粗段/标题段；图片仅在功能点 `.2 原型图` 段写 `IMG-xx` 编号。
- [ ] **R-B PRD 固定 4 章**：仅含 1 开发目的 / 2 版本变更 / 3 术语 / 4 功能需求；**未**另起 `## 5 功能点明细` 或其它平行章；功能点直接挂在第 4 章对应末级菜单（`##### 4.4.x.y` 下紧跟 `###### 4.4.x.y.z`）。
- [ ] **多系统视图 4 层结构**（§1.3.1）：用户输入多个系统视图时，第 4 章按 4 层结构展开——`4.4.X = 系统视图` / `4.4.X.Y = 一级菜单` / `4.4.X.Y.Z = 末级菜单` / `4.4.X.Y.Z.W = 功能点`；**同名菜单跨系统不合并**（即使 WMS 和 OMS 都有「物流-平台物流」，必须各自独立写完整 7 项用例规则）；**§4.3.2 系统视图边界**必填，列出每个系统范围 + 跨系统数据/调用关系；**单系统视图**省略 §4.3.2，4.4.X 直接为一级菜单（3 层结构）。
- [ ] **版本变更表标准 4 列**(§1.4)：PRD §2 版本变更表列固定为 `版本 / 日期 / 变更人 / 变更内容`；版本号 `vX.Y` 格式；日期 `YYYY-MM-DD`；变更内容含具体模块/功能点名（如 `商品库：新增"发布到门店"`），多条用 `；` 分隔，动词领起（新增/优化/删除/修复），**禁**泛化（"优化体验"等）。
- [ ] **术语章节标配清单**(§1.5)：PRD §3 术语章节**必含**以下术语定义（按 PRD 涉及动作触发）——`软删除`（删除类）/`唯一性校验`（有唯一约束）/`状态机`（含状态流转）/`二次确认弹窗`（含删除/启用/停用/上下架等）/`异步任务`（含导入/导出/批量）/`双消息`（含发布到门店/上架/下架/改价等）/`回显`（含编辑/复制）/`条件必填`（含条件必填字段）；功能点正文用 `（详见术语：<术语名>）` 引用，**禁**重复抄写术语全文。
- [ ] **业务流程图 mermaid 规范**(§2.1)：使用 mermaid 类型恰当（flowchart 主子流程 / stateDiagram-v2 状态机 / sequenceDiagram 时序）；节点形状约定（开始结束圆角 / 动作矩形 / 判断菱形）；泳道用 `subgraph` 模拟；连线区分（普通 `-->` / 条件 `-->|X|` / 反向 `-.->` / 强依赖 `==>`）；单图 ≤ 20 节点（超出拆子流程）；节点文字 ≤ 12 字；**禁**仅文字描述无 mermaid。
- [ ] **「待确认项」标准化**(§1.6)：遇 5 大触发场景（图片相关 / 业务规则缺失 / 菜单功能点不匹配 / 模板库不命中（§3.1.4/§3.1.5/§4.6.5）/ 多种合理解读）必须输出待确认项；统一 **5 列结构**：`编号 / 问题类型 / 涉及范围 / AI 当前处理 / 待用户确认`；逐项 `Q-01、Q-02` 顺序编号；**严禁臆测/推托/自创模板**；输出位置仅在对话辅助段，**严禁写入 PRD 正文文件**；用户答复后必须更新 PRD + 移除该项；评审前所有待确认项必须澄清闭环。
- [ ] **R-C 字段规范仅收录数据字段**：见下方「可执行性」段同名细则。

## 完整性

- [ ] 背景、问题、目标用户已说明。
- [ ] 目标与成功指标可量化。
- [ ] 本期范围与非目标已明确区分。
- [ ] 每条功能需求都有验收标准。
- [ ] 已包含关键流程和异常流程。
- [ ] 若用户当次要求「完整不合并」：功能点明细是否已**逐菜单、逐功能点**写全，未使用「同上/参见/合并」？此模式下是否**未**误用单行「请参考」代替整段？

## 可执行性

- [ ] 需求描述可直接拆解为开发任务。
- [ ] 验收条件可测试、可判定通过/失败。
- [ ] 优先级有依据，且与业务目标一致。
- [ ] 字段规范表内**仅**收录数据字段（文本/下拉/单选/复选/上传/日期/数字 等），**未**出现操作按钮（保存/取消/确定/重置/关闭/复制/导出/导入/打印/启用/停用/删除/新增/编辑 等）、操作链接、底部操作区/弹窗底栏整段、关闭图标✕等；按钮交互均在 `用例规则-操作流程` 中描述。
- [ ] **字段规范五列表头**(§3.1)：表头为 `字段名称 / 类型 / 是否必填/必选 / 默认值 / 约束规则`；`是否必填/必选` 列取值仅限 `是 / 否 / 条件必填`；**严格以截图红 `*` 为准**（无 `*` 必为 `否`，**不做"业务上应必填"的推测**）；`必填=否` 时约束规则**不含** `不能为空` 字样；`默认值` 列照原型显示填写（§3.1.5：有默认值写默认值、无默认值·有占位写占位原文一字照原型、无默认值·无占位写 `空`/`无`；禁 `空（显示"X"）` 旧包装）；**未**出现旧 3 列结构 `约束（默认值/是否必填/规则）`。
- [ ] **字段规范分块标题禁臆造**(§3.1.6)：分块标题（如 `**1、基本信息**` / `**2、价格库存**`）必须有截图依据——弹窗 Tab 标签 / 页面分组 / 折叠面板等可见 UI 元素；分块名**一字不易取自截图真实文字**（如截图 Tab 为"基础信息"则 PRD 必写"基础信息"，禁止改为"基本信息"）；**未**出现凭空自创的分块（如截图无"税务信息"却写 `**4、税务信息**`）；截图无明显分组特征时字段规范**直接平铺为单表**，**未**加任何 `**N、xxx**` 序号分块。
- [ ] **日期时间显示格式按部署地区**(§3.1.5)：开始写 PRD 前**已主动询问用户部署地区**（"本次 PRD 是面向中国还是巴西/其他地区？"）；UI 展示层日期时间格式按部署地区——CN→`YYYY-MM-DD HH:mm:ss`、BR→`DD/MM/YYYY HH:mm:ss`、US→`MM/DD/YYYY hh:mm:ss AM/PM`；数据存储/日志层固定 ISO 8601 `YYYY-MM-DD HH:mm:ss`；用户输入含 `巴西`/`Brasil`/`BRL`/`NCM`/`CEST`/`CFOP`/`葡语` 等线索时已正确判定 BR；**未**未询问就按 CN 默认输出。
- [ ] **约束规则按 UI 控件类型分流推导**(§3.1.4)：
  - **输入类字段**（文本/数字/金额/日期/上传 等）：按行业标准推导（如手机号写 `11 位纯数字；以 1 开头；第 2 位为 3~9`、邮箱 `符合 RFC 5322 格式`），**未**写"按业务定""按预设规则"等推托语。
  - **选择类字段**（下拉/单选/复选/级联/树形 等）：以**截图实际选项**为准；**未**套用行业模板；截图可见选项时直接列举（如 `从"实物商品/虚拟商品"中选择`），截图仅显示占位时写 `从预设的<X>选项中选择`，带「新建 X」时写 `可从已有<X>中选择，或通过"新建<X>"添加`，级联类写 `必须选择到最低层级`。
- [ ] **约束规则禁写内容**(§3.1.4)：字段约束规则列**未**出现 `防 SQL 注入`/`防 XSS`/`防注入` 等通用安全约束（应在 PRD 顶部非功能要求段统一声明）；**未**出现 `失焦或提交时...`/`则拦截保存`/`系统不落库` 等校验逻辑包装（属校验规则或提示消息表）；**未**出现 `以 IMG-XX 为准`/`见 IMG-XX` 等 IMG 引用；**未**出现 `按业务定`/`以实现为准`/`按截图实际值`/`按业务配置` 等推托语；**未**出现 `红字提示`/`拦截保存`/`不落库` 等 UI 反馈描述；**未**出现 `仅在<X>时为必填` 等校验条件前缀。
- [ ] **默认值推导按业务惯例 + UI 控件类型**(§3.1.5)：默认值列**未**出现 `按业务定`/`按业务配置`/`按截图实际值`/`以 IMG-XX 为准`/`按实现` 等推托语；按新定义（§3.1.5）填——有默认值写默认值、无默认值·有占位写占位原文（一字照原型，如 `请选择`/`请输入<字段名>`/`请选择日期`）、无默认值·无占位写 `空`/`无`，**禁 `空（显示"X"）` 旧包装**；状态机字段填初始态（草稿/未支付/待审核 等）；商品类按行业惯例（如商品类型=实物商品、销售模式=现货、保质期管理=关闭、物流模式=仓配）；时区/语言/币种按部署地区。
- [ ] **「查看」场景判定以截图为准**（详见 [prd-template-structure.mdc §3.3.1](../_rules/prd-template-structure.mdc)）：列表截图中单据号显示为蓝色可点击链接 **或** 操作列有「查看」按钮 → 单列 `查看` 功能点描述详情页只读字段；两者均无 → **不单列**，查看口径并入 `查询` 节（**仅四类例外**：权限差异 / 审计强制 / 跨菜单跳转 / 本菜单无独立查询节——此类把"查看"重命名为"查询"）；**未**出现空壳查看节、未重复列表字段表。
- [ ] **详情页查看操作流程三步固定文案**(§3.3.1.1)：操作流程严格为 ① `用户在<模块>列表，点击操作列的「查看」或<数据标识>，系统进入查看界面。` ② `用户浏览只读详情信息。` ③ `用户点击「取消」或关闭，系统将不进行任何操作并返回列表或关闭浮层。`；除占位符外不得替换同义词、不得增删步骤；`<数据标识>` 必须**从用户当次截图识别**真实蓝色链接字段名（按业务对象取值，如商品编号 / 单据号 / 仓库编号 / 部门编号 / 人员编号 等），**禁止**套用默认或上一份 PRD 的取值；**多蓝色链接时必须先向用户二次确认**（不得自作主张全部列出或仅取一个），按用户答复输出；仅有操作列查看按钮、无蓝色链接时省略 `或<数据标识>`。

- [ ] **消息通知按 prompt 清单输出**(§4.6)：AI **未自作主张**添加消息通知；**严格按用户 prompt 中「消息通知清单」处理**——已列出"是"的功能点输出字段表（按 35 类标准模板套用）；未列出/写"无"的功能点统一写 `• 消息通知：无。`；接收人按**两类规则**（§4.6.1）：第 1 类自我通知（创建人/提交人本身）或第 2 类他人触发（受影响相关方）；角色须**具体化**（如"总部商品运营""受影响门店店长""仓库管理员""订单客户"），禁止泛指"用户/相关人员"。
- [ ] **双消息适用范围**(§4.6.4)：业务下发类 4 类（发布到门店/上架/下架/改价格）+ 订单流转类 4 类（发货/签收/退货申请/退货完成）共 8 类**必须双消息**——按【消息 1：操作人确认】+【消息 2:受影响方通知】结构呈现，两张独立表 + 中文小标题；其它 27 类单消息。
- [ ] **消息模板风格**(§4.6.4)：时间 `YYYY-MM-DD HH:mm:ss`；时间介词 `在 <时间>`（禁"已于"）；创建/所属用"你的"；操作动作用"你提交的"；单据全称（禁简称如"订单"）；批量含 `<N> 笔/个`；失败类含原因+下一步；异常类含原因+紧迫度（"请于 X 小时内处理"）；末尾含 `请在「<模块>」查看/处理`；渠道默认站内信不列；发送人不显示。
- [ ] **操作日志「操作明细」字段通用规则**(§4.7.1)：每条"操作明细"示例值按格式 `<动作><对象类型>：<对象唯一标识>=<值>[，<动作详情>]` 写——**必含**对象唯一标识（编号优先+名称辅助）；**编辑类必含**旧值→新值（如 `售价：¥3.5→¥4.0`）；**批量类必含**统计（`共 N 件` 或 `成功 X 条，失败 Y 条`）；**禁止**只写"修改成功"/"批量操作了 N 条"等无标识描述；**禁止**含敏感信息（密码/Token/身份证/银行卡/客户手机号未脱敏）。
- [ ] **操作日志适用范围**(§4.7.2)：查询/查看不输出；C 端商城前端**交易类**（下单/支付/退款/修改账号/修改密码/修改收货地址）**必输出**；C 端**浏览类**（浏览/搜索/筛选/收藏/加入购物车）**不输出**（由用户行为埋点承载）；其它需留痕动作必输出。

## 可评审性

- [ ] 没有模糊词（如"更好""尽量""适当"）未定义。
- [ ] **无 UI 不确定性括注**：操作流程与字段规范中**未**出现 `（若页面提供）` / `（若存在中间弹层）` / `（若提供）` / `（若需要）` / `（若存在）` / `（若有）` 等 UI 不确定括注（仅业务条件存在与否的场景可保留，如"一品多码（若有）"）；所有 UI 元素的存在性已通过截图确认或经用户二次确认后写定。
- [ ] **无推托性括注与措辞**：正文**未**出现 `（以实现为准）` / `（以图为准）` / `（以 IMG-xx 为准）` / `（以模板支持为准）` / `（以状态枚举为准）` / `（以枚举配置为准）` / `（以产品状态规则为准）` / `（多 Tab 或单页滚动，以 IMG-xx 为准）` / `（按需）` / `（可选）` 等括注；**未**出现 `；布局以 IMG-xx 为准` / `按产品设计` / `按业务约定` / `按业务实际` 等嵌入式推托；**未**出现 `或等价入口` / `或等价提交入口` / `或等价状态` 等选项延伸类弱化措辞。
- [ ] **`### 4.3 业务流程图` 节正文无元说明**(§2)：未出现 `• 主流程图：...` / `• 子流程图：...` / `• 泳道角色清单：...` / `• 绘制规范：...` 等元数据描述段；正文仅含 mermaid 流程图本身 + 业务边界口径 + 跨菜单联动规则。
- [ ] **字段规范 / 用例规则内 7 项禁止内嵌 IMG-XX 引用**(§4.3)：字段规范表、字段规范分块标题、字段规范节标题、用例规则 7 项（前置/流程/后置/校验/提示消息/消息通知/操作日志）内**未**出现任何形式的图片编号嵌入——`（IMG-XX）` / `（见 IMG-XX）` / `详见 IMG-XX` / `在 IMG-XX 中` / `通过 IMG-XX` / `含 IMG-XX` / `以 IMG-XX 为准` / `仅 IMG-XX` / `IMG-XX 同类能力` 等；图片标识**仅在 `.2 原型图` 段集中引用**。
- [ ] **入口动作句式按 UI 类型区分**(§4.3.1)：跳转独立页面用 `点击「<按钮名>」按钮，系统进入<新增/编辑/...>页面`；打开弹窗用 `点击「<按钮名>」按钮，系统打开<新增/编辑/...>弹窗`。`按钮` 后缀必加；编辑类前缀必写 `操作列的`；回显类拆为独立短句（`系统打开编辑弹窗，回显数据`）；**未**出现 `系统打开新增/编辑表单` 这种含糊措辞；**未**出现 `点击目标行的「编辑」`。
- [ ] **二次确认 / 配置类弹窗双路径必写**(§4.3.2)：涉及二次确认弹窗（删除/启用/停用/上架/下架/改价/发布/复制 等）和配置类弹窗（新增/编辑/发布到门店/导入/导出/批量编辑 等含「取消」按钮）的操作流程，**已**分别写出「主操作」和「取消」两条路径作为流程结束节点；**正向路径必含 2 要素**——① 成功反馈文案 ② UI 落点（关闭弹窗 / 列表自动刷新 / 状态字段更新 / 返回列表）；取消路径**按入口 UI 类型分流**——**弹窗类**用 `用户点击「取消」，系统关闭弹窗，不执行任何操作。`，**页面类**用 `用户点击「取消」，系统返回<X>列表，不执行任何操作。`，**纯查看弹窗**按 §3.3.1.1 三步固定文案；禁止口语化变体如「将不进行任何操作」「关闭弹窗即可」。
- [ ] **操作流程填字段步骤禁列具体字段名**(§4.3)：操作流程描述"填表/选项"步骤**未**枚举具体字段名（字段已在字段规范表逐行列）；统一句式 `用户填写表单字段，点击「保存」按钮`；多 Tab 写 `用户在"<Tab1>"、"<Tab2>"等 Tab 间切换填写表单字段`；字段联动写括注；**字段 ≤ 2 个**的简单场景可列字段名（如 `用户输入新价格`），**字段 ≥ 3 个一律禁列**。
- [ ] **提示消息表新结构**(§4.5)：列固定为 `字段名称 / 未填写/未选择提示 / 输入错误提示`；**列 2** 仅对必填/条件必填字段填 `请输入<字段名>` 或 `请选择<字段名>`，非必填字段填 `-`；**列 3** 对所有有约束的字段（必填+非必填）填**纯错误提示语本身**（格式/长度/唯一性/范围/枚举/防注入），多条用 `；` 分隔；**严禁混入校验逻辑包装**——**未**出现 `失焦或提交时若...` / `则拦截保存` / `则拦截并` / `系统不落库` / `仅在<X>时为必填` 等校验逻辑/触发/失败后行为描述（列 3 = 纯提示语，不是校验规则）；**禁**整段泛化；与字段规范**逐条对齐**（必填字段必现，非必填但有约束的字段也要现）；**未**出现旧列名 `未输入/选择提示` 或 `其他提示`。
- [ ] **提示消息表"进表/不进表"判定**(§4.5.8)：纯选择类字段（下拉/单选/复选/级联/树形）+ 必填=否 → **不进表**（无错误可能）；纯选择类 + 必填=是 → 进表但列 3 = `-`；文本类字段（文本输入框/数字/金额/日期/上传）→ **必进表**（因可能输入错误）；截图中已有红字错误文案 → 优先采用截图实际文案。
- [ ] **提示消息适用范围严格判定**(§4.5.5)：**仅** `新增 / 编辑 / 改价格 / 设置类` 4 类功能点写字段表，**其它一律写 `无。`**（含查询/查看/删除/启用/停用/上架/下架/发布/导入/导出/打印/复制 等）；非 4 类功能点的"请选择 X""文件错误"等校验提示一律放 §4.4 校验规则承载。
- [ ] **差异升级铁律**(§4.5.5)：AI 写新 PRD 时若遇到边界场景（功能点名不在 4 类白名单但有填表保存动作 / "其它"类截图含字段红字提示 / 截图特征模糊），**未**自作主张归类或扩展规则，**已先在对话框告知用户并请其判断**（写字段表 / 仍 `无。` / 破例处理 / 其它）后才输出。
- [ ] **校验规则与提示消息表职责分离**(§4.4.1)：校验规则**仅写非字段级**校验（跨字段/状态/权限/业务逻辑/异常 5 类）；字段级校验（必填/格式/长度/唯一性/范围/防注入/枚举）**未**重复出现在校验规则中（已由 §4.5 提示消息表承载）；每条规则套用统一句式 `当<触发>时，若<拦截>，则<动作>并提示"<文案>"，系统<失败后行为>`；**1 条同行，2 条及以上必须按 `1、2、3、` 全角顿号分条**（两空格缩进，禁止用 `。` 拼接成一段长文本，与前置/后置条件一致）；无非字段级校验时写 `字段级校验见提示消息表；本功能点无跨字段、状态、权限、业务逻辑或异常类校验。`。
- [ ] **单条校验边界**(§4.4.4)：1 条校验 = 1 个完整"触发→拦截→动作→提示→失败后行为"链；出现**任一**信号必须拆分：字数 ≥ 80 / ≥ 2 个 `当...时` 或 `若...` / `；` 分隔并列校验 / 枚举式罗列；**非校验内容必须移出**——错误反馈机制（下载按钮 / 进度条）→ 操作流程，业务策略（"默认允许 X 策略"）→ 操作流程或后置条件，字段映射 → 操作流程，UI 反馈 → 操作流程或字段规范。
- [ ] **前置条件三要素 + 分条规范**(§4.2.1)：包含 ① 登录（`用户已登录<系统>`）+ ② 权限（`具备「<完整菜单路径>-<功能点名>」权限`）+ ③ 业务前置（按功能点类型必写，如编辑/删除/启用/停用/上下架/改价/发布/打印/复制 必含 `<模块>列表中存在<状态>的<业务对象>`）；**1 条同行，2 条及以上必须按 `1、2、3、` 全角顿号分条**（两空格缩进，禁止用 `；` 拼接）；**未**遗漏业务前置、未用模糊措辞（如"具备权限"无菜单路径）。
- [ ] **后置条件按 15 类标准句式输出**(§4.2.2)：每条必含「结果状态 + 可见位置」（如新增写 `生成一条<对象>数据；可在<模块>列表查询到`、编辑写 `<对象>数据更新；<模块>列表数据同步刷新`）；**未**用模糊措辞（如`进入可售相关状态` / `按规则同步` / `以实现为准`）；删除类**不重复抄写**软删除策略全文，仅写 `<对象>从<模块>列表移除（详见术语：软删除）`；§3 术语章节**已定义**「软删除」条目；**1 条同行，2 条及以上必须按 `1、2、3、` 全角顿号分条**（两空格缩进，禁止用 `；` 拼接）；句末统一全角 `。`。
- [ ] 专有名词前后一致。
- [ ] 留有"待确认问题"供评审闭环。
